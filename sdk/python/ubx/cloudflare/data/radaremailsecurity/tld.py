# Code generated by `ubx sdk gen` from cloudflare_tld. DO NOT EDIT.
from __future__ import annotations

import dataclasses
from typing import Any

import ubx_sdk as ubx

@dataclasses.dataclass
class Tld_Result_Meta_ConfidenceInfo_Annotations:
    data_source: Any = None
    description: Any = None
    end_date: Any = None
    event_type: Any = None
    is_instantaneous: Any = None
    linked_url: Any = None
    start_date: Any = None
    tags: Any = None

@dataclasses.dataclass
class Tld_Result_Meta_ConfidenceInfo:
    annotations: Any = None
    # Provides an indication of how much confidence Cloudflare has in the data.
    level: Any = None

@dataclasses.dataclass
class Tld_Result_Meta_DateRange:
    end_time: Any = None
    start_time: Any = None

@dataclasses.dataclass
class Tld_Result_Meta_Units:
    name: Any = None
    value: Any = None

@dataclasses.dataclass
class Tld_Result_Meta:
    confidence_info: Any = None
    date_range: Any = None
    # Timestamp of the last dataset update.
    last_updated: Any = None
    # Normalization method applied to the results. Refer to [Normalization methods](https://developers.cloudflare.com/radar/concepts/normalization/).
    normalization: Any = None
    # Measurement units for the results.
    units: Any = None

@dataclasses.dataclass
class Tld_Result:
    # Metadata for the results.
    meta: Any = None
    top_0: Any = None

@dataclasses.dataclass
class TldConfig:
    # Filters results by ARC (Authenticated Received Chain) validation.
    arc: Any = None
    # End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.
    date_end: Any = None
    # Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).
    date_range: Any = None
    # Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.
    date_start: Any = None
    # Filters results by DKIM (DomainKeys Identified Mail) validation status.
    dkim: Any = None
    # Filters results by DMARC (Domain-based Message Authentication, Reporting and Conformance) validation status.
    dmarc: Any = None
    # Format in which results will be returned.
    format: Any = None
    # Limits the number of objects returned in the response.
    limit: Any = None
    # Array of names used to label the series in the response.
    name: Any = None
    # Filters results by SPF (Sender Policy Framework) validation status.
    spf: Any = None
    # Filters results by TLD category.
    tld_category: Any = None
    # Filters results by TLS version.
    tls_version: Any = None

@dataclasses.dataclass
class TldAttrs:
    # Filters results by ARC (Authenticated Received Chain) validation.
    arc: Any = None
    # End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.
    date_end: Any = None
    # Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).
    date_range: Any = None
    # Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.
    date_start: Any = None
    # Filters results by DKIM (DomainKeys Identified Mail) validation status.
    dkim: Any = None
    # Filters results by DMARC (Domain-based Message Authentication, Reporting and Conformance) validation status.
    dmarc: Any = None
    # Format in which results will be returned.
    format: Any = None
    # Limits the number of objects returned in the response.
    limit: Any = None
    # Array of names used to label the series in the response.
    name: Any = None
    result: Any = None
    # Filters results by SPF (Sender Policy Framework) validation status.
    spf: Any = None
    success: Any = None
    # Filters results by TLD category.
    tld_category: Any = None
    # Filters results by TLS version.
    tls_version: Any = None

Tld = ubx.DataSourceBinding(
    wire_type="cloudflare_tld",
    fields={
        "arc": ubx.FieldSpec(wire_name="arc"),
        "date_end": ubx.FieldSpec(wire_name="date_end"),
        "date_range": ubx.FieldSpec(wire_name="date_range"),
        "date_start": ubx.FieldSpec(wire_name="date_start"),
        "dkim": ubx.FieldSpec(wire_name="dkim"),
        "dmarc": ubx.FieldSpec(wire_name="dmarc"),
        "format": ubx.FieldSpec(wire_name="format"),
        "limit": ubx.FieldSpec(wire_name="limit"),
        "name": ubx.FieldSpec(wire_name="name"),
        "spf": ubx.FieldSpec(wire_name="spf"),
        "tld_category": ubx.FieldSpec(wire_name="tld_category"),
        "tls_version": ubx.FieldSpec(wire_name="tls_version"),
    },
)

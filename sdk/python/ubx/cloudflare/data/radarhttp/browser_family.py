# Code generated by `ubx sdk gen` from cloudflare_browser_family. DO NOT EDIT.
from __future__ import annotations

import dataclasses
from typing import Any

import ubx_sdk as ubx

@dataclasses.dataclass
class BrowserFamily_Result_Meta_ConfidenceInfo_Annotations:
    data_source: Any = None
    description: Any = None
    end_date: Any = None
    event_type: Any = None
    is_instantaneous: Any = None
    linked_url: Any = None
    start_date: Any = None
    tags: Any = None

@dataclasses.dataclass
class BrowserFamily_Result_Meta_ConfidenceInfo:
    annotations: Any = None
    # Provides an indication of how much confidence Cloudflare has in the data.
    level: Any = None

@dataclasses.dataclass
class BrowserFamily_Result_Meta_DateRange:
    end_time: Any = None
    start_time: Any = None

@dataclasses.dataclass
class BrowserFamily_Result_Meta_Units:
    name: Any = None
    value: Any = None

@dataclasses.dataclass
class BrowserFamily_Result_Meta:
    # Aggregation interval of the results (e.g., in 15 minutes or 1 hour intervals). Refer to [Aggregation intervals](https://developers.cloudflare.com/radar/concepts/aggregation-intervals/).
    agg_interval: Any = None
    confidence_info: Any = None
    date_range: Any = None
    # Timestamp of the last dataset update.
    last_updated: Any = None
    # Normalization method applied to the results. Refer to [Normalization methods](https://developers.cloudflare.com/radar/concepts/normalization/).
    normalization: Any = None
    # Measurement units for the results.
    units: Any = None

@dataclasses.dataclass
class BrowserFamily_Result_Serie0:
    timestamps: Any = None

@dataclasses.dataclass
class BrowserFamily_Result:
    # Metadata for the results.
    meta: Any = None
    serie_0: Any = None

@dataclasses.dataclass
class BrowserFamilyConfig:
    # Aggregation interval of the results (e.g., in 15 minutes or 1 hour intervals). Refer to [Aggregation intervals](https://developers.cloudflare.com/radar/concepts/aggregation-intervals/). When omitted, the interval is auto-selected from the requested date range; finer intervals are only available for shorter ranges. If the requested interval is too granular for the date range, the request is rejected.
    agg_interval: Any = None
    # Filters results by Autonomous System. Specify one or more Autonomous System Numbers (ASNs) as a comma-separated list. Prefix with `-` to exclude ASNs from results. For example, `-174, 3356` excludes results from AS174, but includes results from AS3356.
    asn: Any = None
    # Filters results by bot class. Refer to [Bot classes](https://developers.cloudflare.com/radar/concepts/bot-classes/).
    bot_class: Any = None
    # Filters results by continent. Specify a comma-separated list of alpha-2 codes. Prefix with `-` to exclude continents from results. For example, `-EU,NA` excludes results from EU, but includes results from NA.
    continent: Any = None
    # End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.
    date_end: Any = None
    # Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).
    date_range: Any = None
    # Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.
    date_start: Any = None
    # Filters results by device type.
    device_type: Any = None
    # Format in which results will be returned.
    format: Any = None
    # Filters results by Geolocation. Specify a comma-separated list of GeoNames IDs. Prefix with `-` to exclude geoIds from results. For example, `-2267056,360689` excludes results from the 2267056 (Lisbon), but includes results from 5128638 (New York).
    geo_id: Any = None
    # Filters results by HTTP protocol (HTTP vs. HTTPS).
    http_protocol: Any = None
    # Filters results by HTTP version.
    http_version: Any = None
    # Filters results by IP version (Ipv4 vs. IPv6).
    ip_version: Any = None
    # Limits the number of objects per group to the top items within the specified time range. When item count exceeds the limit, extra items appear grouped under an "other" category. Only supported on high-cardinality dimensions; otherwise the request is rejected. Minimum value is 2.
    limit_per_group: Any = None
    # Filters results by location. Specify a comma-separated list of alpha-2 codes. Prefix with `-` to exclude locations from results. For example, `-US,PT` excludes results from the US, but includes results from PT.
    location: Any = None
    # Array of names used to label the series in the response.
    name: Any = None
    # Filters results by operating system.
    os: Any = None
    # Filters results by TLS version.
    tls_version: Any = None

@dataclasses.dataclass
class BrowserFamilyAttrs:
    # Aggregation interval of the results (e.g., in 15 minutes or 1 hour intervals). Refer to [Aggregation intervals](https://developers.cloudflare.com/radar/concepts/aggregation-intervals/). When omitted, the interval is auto-selected from the requested date range; finer intervals are only available for shorter ranges. If the requested interval is too granular for the date range, the request is rejected.
    agg_interval: Any = None
    # Filters results by Autonomous System. Specify one or more Autonomous System Numbers (ASNs) as a comma-separated list. Prefix with `-` to exclude ASNs from results. For example, `-174, 3356` excludes results from AS174, but includes results from AS3356.
    asn: Any = None
    # Filters results by bot class. Refer to [Bot classes](https://developers.cloudflare.com/radar/concepts/bot-classes/).
    bot_class: Any = None
    # Filters results by continent. Specify a comma-separated list of alpha-2 codes. Prefix with `-` to exclude continents from results. For example, `-EU,NA` excludes results from EU, but includes results from NA.
    continent: Any = None
    # End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.
    date_end: Any = None
    # Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters).
    date_range: Any = None
    # Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected.
    date_start: Any = None
    # Filters results by device type.
    device_type: Any = None
    # Format in which results will be returned.
    format: Any = None
    # Filters results by Geolocation. Specify a comma-separated list of GeoNames IDs. Prefix with `-` to exclude geoIds from results. For example, `-2267056,360689` excludes results from the 2267056 (Lisbon), but includes results from 5128638 (New York).
    geo_id: Any = None
    # Filters results by HTTP protocol (HTTP vs. HTTPS).
    http_protocol: Any = None
    # Filters results by HTTP version.
    http_version: Any = None
    # Filters results by IP version (Ipv4 vs. IPv6).
    ip_version: Any = None
    # Limits the number of objects per group to the top items within the specified time range. When item count exceeds the limit, extra items appear grouped under an "other" category. Only supported on high-cardinality dimensions; otherwise the request is rejected. Minimum value is 2.
    limit_per_group: Any = None
    # Filters results by location. Specify a comma-separated list of alpha-2 codes. Prefix with `-` to exclude locations from results. For example, `-US,PT` excludes results from the US, but includes results from PT.
    location: Any = None
    # Array of names used to label the series in the response.
    name: Any = None
    # Filters results by operating system.
    os: Any = None
    result: Any = None
    success: Any = None
    # Filters results by TLS version.
    tls_version: Any = None

BrowserFamily = ubx.DataSourceBinding(
    wire_type="cloudflare_browser_family",
    fields={
        "agg_interval": ubx.FieldSpec(wire_name="agg_interval"),
        "asn": ubx.FieldSpec(wire_name="asn"),
        "bot_class": ubx.FieldSpec(wire_name="bot_class"),
        "continent": ubx.FieldSpec(wire_name="continent"),
        "date_end": ubx.FieldSpec(wire_name="date_end"),
        "date_range": ubx.FieldSpec(wire_name="date_range"),
        "date_start": ubx.FieldSpec(wire_name="date_start"),
        "device_type": ubx.FieldSpec(wire_name="device_type"),
        "format": ubx.FieldSpec(wire_name="format"),
        "geo_id": ubx.FieldSpec(wire_name="geo_id"),
        "http_protocol": ubx.FieldSpec(wire_name="http_protocol"),
        "http_version": ubx.FieldSpec(wire_name="http_version"),
        "ip_version": ubx.FieldSpec(wire_name="ip_version"),
        "limit_per_group": ubx.FieldSpec(wire_name="limit_per_group"),
        "location": ubx.FieldSpec(wire_name="location"),
        "name": ubx.FieldSpec(wire_name="name"),
        "os": ubx.FieldSpec(wire_name="os"),
        "tls_version": ubx.FieldSpec(wire_name="tls_version"),
    },
)

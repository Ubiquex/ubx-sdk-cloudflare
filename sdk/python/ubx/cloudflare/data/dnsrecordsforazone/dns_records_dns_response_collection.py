# Code generated by `ubx sdk gen` from cloudflare_dns_records_dns_response_collection. DO NOT EDIT.
from __future__ import annotations

import dataclasses
from typing import Any

import ubx_sdk as ubx

@dataclasses.dataclass
class DnsRecordsDnsResponseCollectionConfig:
    # Exact value of the DNS record comment. This is a convenience alias for `comment.exact`.
    comment: Any = None
    # If this parameter is present, only records *without* a comment are returned.
    comment_absent: Any = None
    # Substring of the DNS record comment. Comment filters are case-insensitive.
    comment_contains: Any = None
    # Suffix of the DNS record comment. Comment filters are case-insensitive.
    comment_endswith: Any = None
    # Exact value of the DNS record comment. Comment filters are case-insensitive.
    comment_exact: Any = None
    # If this parameter is present, only records *with* a comment are returned.
    comment_present: Any = None
    # Prefix of the DNS record comment. Comment filters are case-insensitive.
    comment_startswith: Any = None
    # Exact value of the DNS record content. This is a convenience alias for `content.exact`.
    content: Any = None
    # Substring of the DNS record content. Content filters are case-insensitive.
    content_contains: Any = None
    # Suffix of the DNS record content. Content filters are case-insensitive.
    content_endswith: Any = None
    # Exact value of the DNS record content. Content filters are case-insensitive.
    content_exact: Any = None
    # Prefix of the DNS record content. Content filters are case-insensitive.
    content_startswith: Any = None
    # Direction to order DNS records in.
    direction: Any = None
    # Whether to include shadow metadata in the `meta` field of each record in the response. See [Shadowed records](https://developers.cloudflare.com/dns/manage-dns-records/reference/shadowed-records).
    include_shadow_metadata: Any = None
    # Whether to match all search requirements or at least one (any). If set to `all`, acts like a logical AND between filters. If set to `any`, acts like a logical OR instead. Note that the interaction between tag filters is controlled by the `tag-match` parameter instead.
    match: Any = None
    # Exact value of the DNS record name. This is a convenience alias for `name.exact`.
    name: Any = None
    # Substring of the DNS record name. Name filters are case-insensitive.
    name_contains: Any = None
    # Suffix of the DNS record name. Name filters are case-insensitive.
    name_endswith: Any = None
    # Exact value of the DNS record name. Name filters are case-insensitive.
    name_exact: Any = None
    # Prefix of the DNS record name. Name filters are case-insensitive.
    name_startswith: Any = None
    # Field to order DNS records by.
    order: Any = None
    # Page number of paginated results.
    page: Any = None
    # Number of DNS records per page.
    per_page: Any = None
    # Whether the record is receiving the performance and security benefits of Cloudflare.
    proxied: Any = None
    # Allows searching in multiple properties of a DNS record simultaneously. This parameter is intended for human users, not automation. Its exact behavior is intentionally left unspecified and is subject to change in the future. This parameter works independently of the `match` setting. For automated searches, please use the other available parameters.
    search: Any = None
    # Filters to records at or below the given NS delegation name, excluding the NS records that form the delegation itself. The value must be a subdomain of the zone; the zone apex is not accepted. Requires `include_shadow_metadata=true`. See [Shadowed records](https://developers.cloudflare.com/dns/manage-dns-records/reference/shadowed-records).
    shadowed_by_name: Any = None
    # Returns NS records that shadow the given name, searching at the name itself and each of its ancestor names within the zone, excluding the zone apex. The value must be a subdomain of the zone; the zone apex is not accepted. See [Shadowed records](https://developers.cloudflare.com/dns/manage-dns-records/reference/shadowed-records).
    shadowing_name: Any = None
    # Condition on the DNS record tag. Parameter values can be of the form `<tag-name>:<tag-value>` to search for an exact `name:value` pair, or just `<tag-name>` to search for records with a specific tag name regardless of its value. This is a convenience shorthand for the more powerful `tag.<predicate>` parameters. Examples: - `tag=important` is equivalent to `tag.present=important` - `tag=team:DNS` is equivalent to `tag.exact=team:DNS`
    tag: Any = None
    # Name of a tag which must *not* be present on the DNS record. Tag filters are case-insensitive.
    tag_absent: Any = None
    # A tag and value, of the form `<tag-name>:<tag-value>`. The API will only return DNS records that have a tag named `<tag-name>` whose value contains `<tag-value>`. Tag filters are case-insensitive.
    tag_contains: Any = None
    # A tag and value, of the form `<tag-name>:<tag-value>`. The API will only return DNS records that have a tag named `<tag-name>` whose value ends with `<tag-value>`. Tag filters are case-insensitive.
    tag_endswith: Any = None
    # A tag and value, of the form `<tag-name>:<tag-value>`. The API will only return DNS records that have a tag named `<tag-name>` whose value is `<tag-value>`. Tag filters are case-insensitive.
    tag_exact: Any = None
    # Whether to match all tag search requirements or at least one (any). If set to `all`, acts like a logical AND between tag filters. If set to `any`, acts like a logical OR instead. Note that the regular `match` parameter is still used to combine the resulting condition with other filters that aren't related to tags.
    tag_match: Any = None
    # Name of a tag which must be present on the DNS record. Tag filters are case-insensitive.
    tag_present: Any = None
    # A tag and value, of the form `<tag-name>:<tag-value>`. The API will only return DNS records that have a tag named `<tag-name>` whose value starts with `<tag-value>`. Tag filters are case-insensitive.
    tag_startswith: Any = None
    # Record type.
    type: Any = None
    # Identifier.
    zone_id: Any = None

@dataclasses.dataclass
class DnsRecordsDnsResponseCollectionAttrs:
    # Exact value of the DNS record comment. This is a convenience alias for `comment.exact`.
    comment: Any = None
    # If this parameter is present, only records *without* a comment are returned.
    comment_absent: Any = None
    # Substring of the DNS record comment. Comment filters are case-insensitive.
    comment_contains: Any = None
    # Suffix of the DNS record comment. Comment filters are case-insensitive.
    comment_endswith: Any = None
    # Exact value of the DNS record comment. Comment filters are case-insensitive.
    comment_exact: Any = None
    # If this parameter is present, only records *with* a comment are returned.
    comment_present: Any = None
    # Prefix of the DNS record comment. Comment filters are case-insensitive.
    comment_startswith: Any = None
    # Exact value of the DNS record content. This is a convenience alias for `content.exact`.
    content: Any = None
    # Substring of the DNS record content. Content filters are case-insensitive.
    content_contains: Any = None
    # Suffix of the DNS record content. Content filters are case-insensitive.
    content_endswith: Any = None
    # Exact value of the DNS record content. Content filters are case-insensitive.
    content_exact: Any = None
    # Prefix of the DNS record content. Content filters are case-insensitive.
    content_startswith: Any = None
    # Direction to order DNS records in.
    direction: Any = None
    # Whether to include shadow metadata in the `meta` field of each record in the response. See [Shadowed records](https://developers.cloudflare.com/dns/manage-dns-records/reference/shadowed-records).
    include_shadow_metadata: Any = None
    # Whether to match all search requirements or at least one (any). If set to `all`, acts like a logical AND between filters. If set to `any`, acts like a logical OR instead. Note that the interaction between tag filters is controlled by the `tag-match` parameter instead.
    match: Any = None
    # Exact value of the DNS record name. This is a convenience alias for `name.exact`.
    name: Any = None
    # Substring of the DNS record name. Name filters are case-insensitive.
    name_contains: Any = None
    # Suffix of the DNS record name. Name filters are case-insensitive.
    name_endswith: Any = None
    # Exact value of the DNS record name. Name filters are case-insensitive.
    name_exact: Any = None
    # Prefix of the DNS record name. Name filters are case-insensitive.
    name_startswith: Any = None
    # Field to order DNS records by.
    order: Any = None
    # Page number of paginated results.
    page: Any = None
    # Number of DNS records per page.
    per_page: Any = None
    # Whether the record is receiving the performance and security benefits of Cloudflare.
    proxied: Any = None
    # Allows searching in multiple properties of a DNS record simultaneously. This parameter is intended for human users, not automation. Its exact behavior is intentionally left unspecified and is subject to change in the future. This parameter works independently of the `match` setting. For automated searches, please use the other available parameters.
    search: Any = None
    # Filters to records at or below the given NS delegation name, excluding the NS records that form the delegation itself. The value must be a subdomain of the zone; the zone apex is not accepted. Requires `include_shadow_metadata=true`. See [Shadowed records](https://developers.cloudflare.com/dns/manage-dns-records/reference/shadowed-records).
    shadowed_by_name: Any = None
    # Returns NS records that shadow the given name, searching at the name itself and each of its ancestor names within the zone, excluding the zone apex. The value must be a subdomain of the zone; the zone apex is not accepted. See [Shadowed records](https://developers.cloudflare.com/dns/manage-dns-records/reference/shadowed-records).
    shadowing_name: Any = None
    # Condition on the DNS record tag. Parameter values can be of the form `<tag-name>:<tag-value>` to search for an exact `name:value` pair, or just `<tag-name>` to search for records with a specific tag name regardless of its value. This is a convenience shorthand for the more powerful `tag.<predicate>` parameters. Examples: - `tag=important` is equivalent to `tag.present=important` - `tag=team:DNS` is equivalent to `tag.exact=team:DNS`
    tag: Any = None
    # Name of a tag which must *not* be present on the DNS record. Tag filters are case-insensitive.
    tag_absent: Any = None
    # A tag and value, of the form `<tag-name>:<tag-value>`. The API will only return DNS records that have a tag named `<tag-name>` whose value contains `<tag-value>`. Tag filters are case-insensitive.
    tag_contains: Any = None
    # A tag and value, of the form `<tag-name>:<tag-value>`. The API will only return DNS records that have a tag named `<tag-name>` whose value ends with `<tag-value>`. Tag filters are case-insensitive.
    tag_endswith: Any = None
    # A tag and value, of the form `<tag-name>:<tag-value>`. The API will only return DNS records that have a tag named `<tag-name>` whose value is `<tag-value>`. Tag filters are case-insensitive.
    tag_exact: Any = None
    # Whether to match all tag search requirements or at least one (any). If set to `all`, acts like a logical AND between tag filters. If set to `any`, acts like a logical OR instead. Note that the regular `match` parameter is still used to combine the resulting condition with other filters that aren't related to tags.
    tag_match: Any = None
    # Name of a tag which must be present on the DNS record. Tag filters are case-insensitive.
    tag_present: Any = None
    # A tag and value, of the form `<tag-name>:<tag-value>`. The API will only return DNS records that have a tag named `<tag-name>` whose value starts with `<tag-value>`. Tag filters are case-insensitive.
    tag_startswith: Any = None
    # Record type.
    type: Any = None
    # Identifier.
    zone_id: Any = None

DnsRecordsDnsResponseCollection = ubx.DataSourceBinding(
    wire_type="cloudflare_dns_records_dns_response_collection",
    fields={
        "comment": ubx.FieldSpec(wire_name="comment"),
        "comment_absent": ubx.FieldSpec(wire_name="comment_absent"),
        "comment_contains": ubx.FieldSpec(wire_name="comment_contains"),
        "comment_endswith": ubx.FieldSpec(wire_name="comment_endswith"),
        "comment_exact": ubx.FieldSpec(wire_name="comment_exact"),
        "comment_present": ubx.FieldSpec(wire_name="comment_present"),
        "comment_startswith": ubx.FieldSpec(wire_name="comment_startswith"),
        "content": ubx.FieldSpec(wire_name="content"),
        "content_contains": ubx.FieldSpec(wire_name="content_contains"),
        "content_endswith": ubx.FieldSpec(wire_name="content_endswith"),
        "content_exact": ubx.FieldSpec(wire_name="content_exact"),
        "content_startswith": ubx.FieldSpec(wire_name="content_startswith"),
        "direction": ubx.FieldSpec(wire_name="direction"),
        "include_shadow_metadata": ubx.FieldSpec(wire_name="include_shadow_metadata"),
        "match": ubx.FieldSpec(wire_name="match"),
        "name": ubx.FieldSpec(wire_name="name"),
        "name_contains": ubx.FieldSpec(wire_name="name_contains"),
        "name_endswith": ubx.FieldSpec(wire_name="name_endswith"),
        "name_exact": ubx.FieldSpec(wire_name="name_exact"),
        "name_startswith": ubx.FieldSpec(wire_name="name_startswith"),
        "order": ubx.FieldSpec(wire_name="order"),
        "page": ubx.FieldSpec(wire_name="page"),
        "per_page": ubx.FieldSpec(wire_name="per_page"),
        "proxied": ubx.FieldSpec(wire_name="proxied"),
        "search": ubx.FieldSpec(wire_name="search"),
        "shadowed_by_name": ubx.FieldSpec(wire_name="shadowed_by_name"),
        "shadowing_name": ubx.FieldSpec(wire_name="shadowing_name"),
        "tag": ubx.FieldSpec(wire_name="tag"),
        "tag_absent": ubx.FieldSpec(wire_name="tag_absent"),
        "tag_contains": ubx.FieldSpec(wire_name="tag_contains"),
        "tag_endswith": ubx.FieldSpec(wire_name="tag_endswith"),
        "tag_exact": ubx.FieldSpec(wire_name="tag_exact"),
        "tag_match": ubx.FieldSpec(wire_name="tag_match"),
        "tag_present": ubx.FieldSpec(wire_name="tag_present"),
        "tag_startswith": ubx.FieldSpec(wire_name="tag_startswith"),
        "type": ubx.FieldSpec(wire_name="type"),
        "zone_id": ubx.FieldSpec(wire_name="zone_id"),
    },
)

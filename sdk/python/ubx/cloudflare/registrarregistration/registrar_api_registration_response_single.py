# Code generated by `ubx sdk gen` from cloudflare_registrar_api_registration_response_single. DO NOT EDIT.
from __future__ import annotations

import dataclasses
from typing import Any

import ubx_sdk as ubx

@dataclasses.dataclass
class RegistrarApiRegistrationResponseSingle_Contacts_Administrator_PostalInfo_Address:
    city: Any = None
    country_code: Any = None
    postal_code: Any = None
    state: Any = None
    street: Any = None

@dataclasses.dataclass
class RegistrarApiRegistrationResponseSingle_Contacts_Administrator_PostalInfo:
    address: Any = None
    name: Any = None
    organization: Any = None

@dataclasses.dataclass
class RegistrarApiRegistrationResponseSingle_Contacts_Administrator:
    email: Any = None
    fax: Any = None
    phone: Any = None
    postal_info: Any = None

@dataclasses.dataclass
class RegistrarApiRegistrationResponseSingle_Contacts:
    # Optional administrator contact. Accepted only when the extension schema includes this role. When the registry requires an omitted contact, Cloudflare may derive it from `contacts.registrant`.
    administrator: Any = None
    # Optional billing contact. Accepted only when the extension schema includes this role. When the registry requires an omitted contact, Cloudflare may derive it from `contacts.registrant`.
    billing: Any = None
    # Optional registrant contact. If omitted, the account's default address book entry is used instead.
    registrant: Any = None
    # Optional technical contact. Accepted only when the extension schema includes this role. When the registry requires an omitted contact, Cloudflare may derive it from `contacts.registrant`.
    technical: Any = None

@dataclasses.dataclass
class RegistrarApiRegistrationResponseSingle_Errors_Source:
    pointer: Any = None

@dataclasses.dataclass
class RegistrarApiRegistrationResponseSingle_Errors:
    code: Any = None
    message: Any = None
    source: Any = None

@dataclasses.dataclass
class RegistrarApiRegistrationResponseSingle_Result:
    # Whether automatic renewal occurs before expiration.
    auto_renew: Any = None
    # When the domain was registered. Present when the registration resource exists.
    created_at: Any = None
    # Provides a fully qualified domain name (FQDN), including the extension (e.g., `example.com`, `mybrand.app`). The domain name uniquely identifies a registration. Cloudflare permits only one registration per domain, making the domain name a natural idempotency key for registration requests.
    domain_name: Any = None
    # When the domain registration expires. Ready registrations include this value; only `registration_pending` may return null.
    expires_at: Any = None
    # Whether the domain is locked for transfer.
    locked: Any = None
    # Current WHOIS privacy mode for the registration.
    privacy_mode: Any = None
    # Current registration status. - `active`: The domain operates with an active registration. - `registration_pending`: Registration remains in progress. - `expired`: The domain registration expired. - `suspended`: The registry suspended the domain. - `redemption_period`: The domain entered the redemption grace period. - `pending_delete`: The registry scheduled the domain for deletion.
    status: Any = None

_RegistrarApiRegistrationResponseSingle_Contacts_Administrator_PostalInfo_AddressFields = {
    "city": ubx.FieldSpec(wire_name="city"),
    "country_code": ubx.FieldSpec(wire_name="country_code"),
    "postal_code": ubx.FieldSpec(wire_name="postal_code"),
    "state": ubx.FieldSpec(wire_name="state"),
    "street": ubx.FieldSpec(wire_name="street"),
}

_RegistrarApiRegistrationResponseSingle_Contacts_Administrator_PostalInfoFields = {
    "address": ubx.FieldSpec(
        wire_name="address",
        kind="object",
        fields=_RegistrarApiRegistrationResponseSingle_Contacts_Administrator_PostalInfo_AddressFields,
    ),
    "name": ubx.FieldSpec(wire_name="name"),
    "organization": ubx.FieldSpec(wire_name="organization"),
}

_RegistrarApiRegistrationResponseSingle_Contacts_AdministratorFields = {
    "email": ubx.FieldSpec(wire_name="email"),
    "fax": ubx.FieldSpec(wire_name="fax"),
    "phone": ubx.FieldSpec(wire_name="phone"),
    "postal_info": ubx.FieldSpec(
        wire_name="postal_info",
        kind="object",
        fields=_RegistrarApiRegistrationResponseSingle_Contacts_Administrator_PostalInfoFields,
    ),
}

_RegistrarApiRegistrationResponseSingle_ContactsFields = {
    "administrator": ubx.FieldSpec(
        wire_name="administrator",
        kind="object",
        fields=_RegistrarApiRegistrationResponseSingle_Contacts_AdministratorFields,
    ),
    "billing": ubx.FieldSpec(
        wire_name="billing",
        kind="object",
        fields=_RegistrarApiRegistrationResponseSingle_Contacts_AdministratorFields,
    ),
    "registrant": ubx.FieldSpec(
        wire_name="registrant",
        kind="object",
        fields=_RegistrarApiRegistrationResponseSingle_Contacts_AdministratorFields,
    ),
    "technical": ubx.FieldSpec(
        wire_name="technical",
        kind="object",
        fields=_RegistrarApiRegistrationResponseSingle_Contacts_AdministratorFields,
    ),
}

@dataclasses.dataclass
class RegistrarApiRegistrationResponseSingleConfig:
    # Provides user acknowledgements for a specific extension or premium registration flow. The extension registration schema from the extension discovery endpoint identifies the required keys.
    acknowledgements: Any = None
    # Enable or disable automatic renewal. Defaults to `false` if omitted. Setting this field to `true` is an explicit opt-in authorizing Cloudflare to charge the account's default payment method up to 30 days before domain expiry to renew the domain automatically. Renewal pricing may change over time based on registry pricing.
    auto_renew: Any = None
    # Provides registry-specific contact extension values for the registrant. `GET /accounts/{account_id}/registrar/extensions/{extension}` identifies the required keys and allowed values for each extension in the `registration_schema.properties.contact_extensions` object. Examples include `.us` nexus fields, `.uk` registrant type fields, and `.ca` legal type fields. Omit this object when the extension's registration schema excludes `contact_extensions`.
    contact_extensions: Any = None
    # Provides contact data for the registration request. The per-extension schema from `GET /accounts/{account_id}/registrar/extensions/{extension}` defines the accepted contact roles. Every currently supported extension requires only `contacts.registrant` from API callers. Callers may provide additional roles such as `technical`, `administrator`, and `billing` when the extension schema includes them. When a registry requires an omitted role, Cloudflare may derive that contact from `contacts.registrant`. When the request omits either the entire `contacts` object or `contacts.registrant`, the system uses the account's default address book entry as the registrant contact. The account owner must configure this default at `https://dash.cloudflare.com/{account_id}/domains/registrations`, where they can create or update the address book entry and accept the required agreement. Dashboard settings currently provide the only way to manage address book entries. Without either a default address book entry or a registrant contact, the registration request fails validation.
    contacts: Any = None
    # Provides a fully qualified domain name (FQDN), including the extension (e.g., `example.com`, `mybrand.app`). The domain name uniquely identifies a registration. Cloudflare permits only one registration per domain, making the domain name a natural idempotency key for registration requests.
    domain_name: Any = None
    # Sets the WHOIS privacy mode for the registration. Defaults to `redaction`. - `off`: Disables WHOIS privacy. - `redaction`: Requests WHOIS redaction where the extension supports it. Some extensions exclude privacy and redaction.
    privacy_mode: Any = None
    # Sets the registration term from 1 to 10 years. When omitted, this field defaults to the registry's minimum registration period for the extension. Most extensions require 1 year, while some require longer minimum terms (e.g., `.ai` requires 2 years). Each registry may also enforce its own maximum registration term. A request above that maximum fails. When uncertain, omit this field to use the default.
    years: Any = None
    # path parameter, not part of the API's own resource representation
    account_id: Any = None

@dataclasses.dataclass
class RegistrarApiRegistrationResponseSingleAttrs:
    # Provides user acknowledgements for a specific extension or premium registration flow. The extension registration schema from the extension discovery endpoint identifies the required keys.
    acknowledgements: Any = None
    # Enable or disable automatic renewal. Defaults to `false` if omitted. Setting this field to `true` is an explicit opt-in authorizing Cloudflare to charge the account's default payment method up to 30 days before domain expiry to renew the domain automatically. Renewal pricing may change over time based on registry pricing.
    auto_renew: Any = None
    # Provides registry-specific contact extension values for the registrant. `GET /accounts/{account_id}/registrar/extensions/{extension}` identifies the required keys and allowed values for each extension in the `registration_schema.properties.contact_extensions` object. Examples include `.us` nexus fields, `.uk` registrant type fields, and `.ca` legal type fields. Omit this object when the extension's registration schema excludes `contact_extensions`.
    contact_extensions: Any = None
    # Provides contact data for the registration request. The per-extension schema from `GET /accounts/{account_id}/registrar/extensions/{extension}` defines the accepted contact roles. Every currently supported extension requires only `contacts.registrant` from API callers. Callers may provide additional roles such as `technical`, `administrator`, and `billing` when the extension schema includes them. When a registry requires an omitted role, Cloudflare may derive that contact from `contacts.registrant`. When the request omits either the entire `contacts` object or `contacts.registrant`, the system uses the account's default address book entry as the registrant contact. The account owner must configure this default at `https://dash.cloudflare.com/{account_id}/domains/registrations`, where they can create or update the address book entry and accept the required agreement. Dashboard settings currently provide the only way to manage address book entries. Without either a default address book entry or a registrant contact, the registration request fails validation.
    contacts: Any = None
    # Provides a fully qualified domain name (FQDN), including the extension (e.g., `example.com`, `mybrand.app`). The domain name uniquely identifies a registration. Cloudflare permits only one registration per domain, making the domain name a natural idempotency key for registration requests.
    domain_name: Any = None
    errors: Any = None
    messages: Any = None
    # Sets the WHOIS privacy mode for the registration. Defaults to `redaction`. - `off`: Disables WHOIS privacy. - `redaction`: Requests WHOIS redaction where the extension supports it. Some extensions exclude privacy and redaction.
    privacy_mode: Any = None
    # A domain registration resource representing the current state of a registered domain.
    result: Any = None
    # Whether the API call was successful.
    success: Any = None
    # Sets the registration term from 1 to 10 years. When omitted, this field defaults to the registry's minimum registration period for the extension. Most extensions require 1 year, while some require longer minimum terms (e.g., `.ai` requires 2 years). Each registry may also enforce its own maximum registration term. A request above that maximum fails. When uncertain, omit this field to use the default.
    years: Any = None
    # path parameter, not part of the API's own resource representation
    account_id: Any = None

RegistrarApiRegistrationResponseSingle = ubx.ResourceBinding(
    wire_type="cloudflare_registrar_api_registration_response_single",
    fields={
        "acknowledgements": ubx.FieldSpec(wire_name="acknowledgements"),
        "auto_renew": ubx.FieldSpec(wire_name="auto_renew"),
        "contact_extensions": ubx.FieldSpec(wire_name="contact_extensions"),
        "contacts": ubx.FieldSpec(
            wire_name="contacts",
            kind="object",
            fields=_RegistrarApiRegistrationResponseSingle_ContactsFields,
        ),
        "domain_name": ubx.FieldSpec(wire_name="domain_name"),
        "privacy_mode": ubx.FieldSpec(wire_name="privacy_mode"),
        "years": ubx.FieldSpec(wire_name="years"),
        "account_id": ubx.FieldSpec(wire_name="account_id"),
    },
)

// Code generated by `ubx sdk gen` from cloudflare_load_balancing_load_balancer_components_schemas_single_response. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface LoadBalancingLoadBalancerComponentsSchemasSingleResponse_AdaptiveRouting {
  /** Extends zero-downtime failover of requests to healthy origins from alternate pools, when no healthy alternate exists in the same pool, according to the failover order defined by traffic and origin steering. When set false (the default) zero-downtime failover will only occur between origins within the same pool. See `session_affinity_attributes` for control over when sessions are broken or reassigned. */
  failoverAcrossPools?: boolean | Computed<boolean>;
}

export interface LoadBalancingLoadBalancerComponentsSchemasSingleResponse_LocationStrategy {
  /** Determines the authoritative location when ECS is not preferred, does not exist in the request, or its GeoIP lookup is unsuccessful. - `"pop"`: Use the Cloudflare PoP location. - `"resolver_ip"`: Use the DNS resolver GeoIP location. If the GeoIP lookup is unsuccessful, use the Cloudflare PoP location. */
  mode?: string | Computed<string>;
  /** Whether the EDNS Client Subnet (ECS) GeoIP should be preferred as the authoritative location. - `"always"`: Always prefer ECS. - `"never"`: Never prefer ECS. - `"proximity"`: Prefer ECS only when `steering_policy="proximity"`. - `"geo"`: Prefer ECS only when `steering_policy="geo"`. */
  preferEcs?: string | Computed<string>;
}

export interface LoadBalancingLoadBalancerComponentsSchemasSingleResponse_RandomSteering {
  /** The default weight for pools in the load balancer that are not specified in the pool_weights map. */
  defaultWeight?: number | Computed<number>;
  /** A mapping of pool IDs to custom weights. The weight is relative to other pools in the load balancer. */
  poolWeights?: Record<string, number> | Computed<Record<string, number>>;
}

export interface LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result_PoolSets_FixedResponse {
  contentType?: string | Computed<string>;
  location?: string | Computed<string>;
  messageBody?: string | Computed<string>;
  statusCode?: number | Computed<number>;
}

export interface LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result_PoolSets_Match_Topology {
  countries?: string[] | Computed<string[]>;
  pops?: string[] | Computed<string[]>;
  regions?: string[] | Computed<string[]>;
}

export interface LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result_PoolSets_Match {
  default?: boolean | Computed<boolean>;
  topology?: LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result_PoolSets_Match_Topology | Computed<LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result_PoolSets_Match_Topology>;
}

export interface LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result_PoolSets_Overrides {
  fallbackPool?: string | Computed<string>;
  poolDefaultWeight?: number | Computed<number>;
  poolWeights?: Record<string, number> | Computed<Record<string, number>>;
  pools?: string[] | Computed<string[]>;
  steeringPolicy?: string | Computed<string>;
}

export interface LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result_PoolSets {
  disabled?: boolean | Computed<boolean>;
  fixedResponse?: LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result_PoolSets_FixedResponse | Computed<LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result_PoolSets_FixedResponse>;
  match?: LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result_PoolSets_Match | Computed<LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result_PoolSets_Match>;
  name?: string | Computed<string>;
  overrides?: LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result_PoolSets_Overrides | Computed<LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result_PoolSets_Overrides>;
}

export interface LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result_Rules_Overrides_SessionAffinityAttributes {
  drainDuration?: number | Computed<number>;
  headers?: string[] | Computed<string[]>;
  requireAllHeaders?: boolean | Computed<boolean>;
  samesite?: string | Computed<string>;
  secure?: string | Computed<string>;
  zeroDowntimeFailover?: string | Computed<string>;
}

export interface LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result_Rules_Overrides {
  adaptiveRouting?: LoadBalancingLoadBalancerComponentsSchemasSingleResponse_AdaptiveRouting | Computed<LoadBalancingLoadBalancerComponentsSchemasSingleResponse_AdaptiveRouting>;
  countryPools?: Record<string, string[]> | Computed<Record<string, string[]>>;
  defaultPools?: string[] | Computed<string[]>;
  fallbackPool?: string | Computed<string>;
  locationStrategy?: LoadBalancingLoadBalancerComponentsSchemasSingleResponse_LocationStrategy | Computed<LoadBalancingLoadBalancerComponentsSchemasSingleResponse_LocationStrategy>;
  poolDefaultWeight?: number | Computed<number>;
  poolWeights?: Record<string, number> | Computed<Record<string, number>>;
  pools?: string[] | Computed<string[]>;
  popPools?: Record<string, string[]> | Computed<Record<string, string[]>>;
  randomSteering?: LoadBalancingLoadBalancerComponentsSchemasSingleResponse_RandomSteering | Computed<LoadBalancingLoadBalancerComponentsSchemasSingleResponse_RandomSteering>;
  regionPools?: Record<string, string[]> | Computed<Record<string, string[]>>;
  sessionAffinity?: string | Computed<string>;
  sessionAffinityAttributes?: LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result_Rules_Overrides_SessionAffinityAttributes | Computed<LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result_Rules_Overrides_SessionAffinityAttributes>;
  sessionAffinityTtl?: number | Computed<number>;
  steeringPolicy?: string | Computed<string>;
  ttl?: number | Computed<number>;
}

export interface LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result_Rules {
  condition?: string | Computed<string>;
  disabled?: boolean | Computed<boolean>;
  fixedResponse?: LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result_PoolSets_FixedResponse | Computed<LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result_PoolSets_FixedResponse>;
  name?: string | Computed<string>;
  overrides?: LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result_Rules_Overrides | Computed<LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result_Rules_Overrides>;
  priority?: number | Computed<number>;
  terminates?: boolean | Computed<boolean>;
}

export interface LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result {
  /** Controls features that modify the routing of requests to pools and origins in response to dynamic conditions, such as during the interval between active health monitoring requests. For example, zero-downtime failover occurs immediately when an origin becomes unavailable due to HTTP 521, 522, or 523 response codes. If there is another healthy origin in the same pool, the request is retried once against this alternate origin. */
  adaptiveRouting?: LoadBalancingLoadBalancerComponentsSchemasSingleResponse_AdaptiveRouting | Computed<LoadBalancingLoadBalancerComponentsSchemasSingleResponse_AdaptiveRouting>;
  /** A mapping of country codes to a list of pool IDs (ordered by their failover priority) for the given country. Any country not explicitly defined will fall back to using the corresponding region_pool mapping if it exists else to default_pools. */
  countryPools?: Record<string, string[]> | Computed<Record<string, string[]>>;
  createdOn?: string | Computed<string>;
  /** A list of pool IDs ordered by their failover priority. Pools defined here are used by default, or when region_pools are not configured for a given region. */
  defaultPools?: string[] | Computed<string[]>;
  /** Object description. */
  description?: string | Computed<string>;
  /** Whether to enable (the default) this load balancer. */
  enabled?: boolean | Computed<boolean>;
  /** The pool ID to use when all other pools are detected as unhealthy. */
  fallbackPool?: string | Computed<string>;
  id?: string | Computed<string>;
  /** Controls location-based steering for non-proxied requests. See `steering_policy` to learn how steering is affected. */
  locationStrategy?: LoadBalancingLoadBalancerComponentsSchemasSingleResponse_LocationStrategy | Computed<LoadBalancingLoadBalancerComponentsSchemasSingleResponse_LocationStrategy>;
  modifiedOn?: string | Computed<string>;
  /** The DNS hostname to associate with your Load Balancer. If this hostname already exists as a DNS record in Cloudflare's DNS, the Load Balancer will take precedence and the DNS record will not be used. */
  name?: string | Computed<string>;
  /** List of networks where Load Balancer or Pool is enabled. */
  networks?: string[] | Computed<string[]>;
  /** An optional list of pool sets, evaluated in array order with first match wins. Pool sets are independent from the standard steering fields (`region_pools` / `country_pools` / `pop_pools` / `default_pools` / `steering_policy` / `random_steering` / `fallback_pool` / `rules`). On a PATCH, an empty array (`pool_sets: []`) clears all pool sets, while omitting the field leaves existing pool sets unchanged. */
  poolSets?: LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result_PoolSets[] | Computed<LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result_PoolSets[]>;
  /** Enterprise only: A mapping of Cloudflare PoP identifiers to a list of pool IDs (ordered by their failover priority) for the PoP (datacenter). Any PoPs not explicitly defined will fall back to using the corresponding country_pool, then region_pool mapping if it exists else to default_pools. */
  popPools?: Record<string, string[]> | Computed<Record<string, string[]>>;
  /** Whether the hostname should be gray clouded (false) or orange clouded (true). */
  proxied?: boolean | Computed<boolean>;
  /** Configures pool weights. - `steering_policy="random"`: A random pool is selected with probability proportional to pool weights. - `steering_policy="least_outstanding_requests"`: Use pool weights to scale each pool's outstanding requests. - `steering_policy="least_connections"`: Use pool weights to scale each pool's open connections. */
  randomSteering?: LoadBalancingLoadBalancerComponentsSchemasSingleResponse_RandomSteering | Computed<LoadBalancingLoadBalancerComponentsSchemasSingleResponse_RandomSteering>;
  /** A mapping of region codes to a list of pool IDs (ordered by their failover priority) for the given region. Any regions not explicitly defined will fall back to using default_pools. */
  regionPools?: Record<string, string[]> | Computed<Record<string, string[]>>;
  /** BETA Field Not General Access: A list of rules for this load balancer to execute. */
  rules?: LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result_Rules[] | Computed<LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result_Rules[]>;
  /** Specifies the type of session affinity the load balancer should use unless specified as `"none"`. The supported types are: - `"cookie"`: On the first request to a proxied load balancer, a cookie is generated, encoding information of which origin the request will be forwarded to. Subsequent requests, by the same client to the same load balancer, will be sent to the origin server the cookie encodes, for the duration of the cookie and as long as the origin server remains healthy. If the cookie has expired or the origin server is unhealthy, then a new origin server is calculated and used. - `"ip_cookie"`: Behaves the same as `"cookie"` except the initial origin selection is stable and based on the client's ip address. - `"header"`: On the first request to a proxied load balancer, a session key based on the configured HTTP headers (see `session_affinity_attributes.headers`) is generated, encoding the request headers used for storing in the load balancer session state which origin the request will be forwarded to. Subsequent requests to the load balancer with the same headers will be sent to the same origin server, for the duration of the session and as long as the origin server remains healthy. If the session has been idle for the duration of `session_affinity_ttl` seconds or the origin server is unhealthy, then a new origin server is calculated and used. See `headers` in `session_affinity_attributes` for additional required configuration. */
  sessionAffinity?: string | Computed<string>;
  /** Configures attributes for session affinity. */
  sessionAffinityAttributes?: LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result_Rules_Overrides_SessionAffinityAttributes | Computed<LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result_Rules_Overrides_SessionAffinityAttributes>;
  /** Time, in seconds, until a client's session expires after being created. Once the expiry time has been reached, subsequent requests may get sent to a different origin server. The accepted ranges per `session_affinity` policy are: - `"cookie"` / `"ip_cookie"`: The current default of 23 hours will be used unless explicitly set. The accepted range of values is between [1800, 604800]. - `"header"`: The current default of 1800 seconds will be used unless explicitly set. The accepted range of values is between [30, 3600]. Note: With session affinity by header, sessions only expire after they haven't been used for the number of seconds specified. */
  sessionAffinityTtl?: number | Computed<number>;
  /** Steering Policy for this load balancer. - `"off"`: Use `default_pools`. - `"geo"`: Use `region_pools`/`country_pools`/`pop_pools`. For non-proxied requests, the country for `country_pools` is determined by `location_strategy`. - `"random"`: Select a pool randomly. - `"dynamic_latency"`: Use round trip time to select the closest pool in default_pools (requires pool health checks). - `"proximity"`: Use the pools' latitude and longitude to select the closest pool using the Cloudflare PoP location for proxied requests or the location determined by `location_strategy` for non-proxied requests. - `"least_outstanding_requests"`: Select a pool by taking into consideration `random_steering` weights, as well as each pool's number of outstanding requests. Pools with more pending requests are weighted proportionately less relative to others. - `"least_connections"`: Select a pool by taking into consideration `random_steering` weights, as well as each pool's number of open connections. Pools with more open connections are weighted proportionately less relative to others. Supported for HTTP/1 and HTTP/2 connections. - `""`: Will map to `"geo"` if you use `region_pools`/`country_pools`/`pop_pools` otherwise `"off"`. */
  steeringPolicy?: string | Computed<string>;
  /** Time to live (TTL) of the DNS entry for the IP address returned by this load balancer. This only applies to gray-clouded (unproxied) load balancers. */
  ttl?: number | Computed<number>;
  zoneName?: string | Computed<string>;
}

const LoadBalancingLoadBalancerComponentsSchemasSingleResponse_AdaptiveRoutingFields: FieldMap = {
  failoverAcrossPools: "failover_across_pools",
};

const LoadBalancingLoadBalancerComponentsSchemasSingleResponse_LocationStrategyFields: FieldMap = {
  mode: "mode",
  preferEcs: "prefer_ecs",
};

const LoadBalancingLoadBalancerComponentsSchemasSingleResponse_RandomSteeringFields: FieldMap = {
  defaultWeight: "default_weight",
  poolWeights: "pool_weights",
};

const LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result_PoolSets_FixedResponseFields: FieldMap = {
  contentType: "content_type",
  location: "location",
  messageBody: "message_body",
  statusCode: "status_code",
};

const LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result_Rules_Overrides_SessionAffinityAttributesFields: FieldMap = {
  drainDuration: "drain_duration",
  headers: "headers",
  requireAllHeaders: "require_all_headers",
  samesite: "samesite",
  secure: "secure",
  zeroDowntimeFailover: "zero_downtime_failover",
};

const LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result_Rules_OverridesFields: FieldMap = {
  adaptiveRouting: {
    wireName: "adaptive_routing",
    kind: "object",
    fields: LoadBalancingLoadBalancerComponentsSchemasSingleResponse_AdaptiveRoutingFields,
  },
  countryPools: "country_pools",
  defaultPools: "default_pools",
  fallbackPool: "fallback_pool",
  locationStrategy: {
    wireName: "location_strategy",
    kind: "object",
    fields: LoadBalancingLoadBalancerComponentsSchemasSingleResponse_LocationStrategyFields,
  },
  poolDefaultWeight: "pool_default_weight",
  poolWeights: "pool_weights",
  pools: "pools",
  popPools: "pop_pools",
  randomSteering: {
    wireName: "random_steering",
    kind: "object",
    fields: LoadBalancingLoadBalancerComponentsSchemasSingleResponse_RandomSteeringFields,
  },
  regionPools: "region_pools",
  sessionAffinity: "session_affinity",
  sessionAffinityAttributes: {
    wireName: "session_affinity_attributes",
    kind: "object",
    fields: LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result_Rules_Overrides_SessionAffinityAttributesFields,
  },
  sessionAffinityTtl: "session_affinity_ttl",
  steeringPolicy: "steering_policy",
  ttl: "ttl",
};

const LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result_RulesFields: FieldMap = {
  condition: "condition",
  disabled: "disabled",
  fixedResponse: {
    wireName: "fixed_response",
    kind: "object",
    fields: LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result_PoolSets_FixedResponseFields,
  },
  name: "name",
  overrides: {
    wireName: "overrides",
    kind: "object",
    fields: LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result_Rules_OverridesFields,
  },
  priority: "priority",
  terminates: "terminates",
};

export interface LoadBalancingLoadBalancerComponentsSchemasSingleResponseConfig {
  /** Controls features that modify the routing of requests to pools and origins in response to dynamic conditions, such as during the interval between active health monitoring requests. For example, zero-downtime failover occurs immediately when an origin becomes unavailable due to HTTP 521, 522, or 523 response codes. If there is another healthy origin in the same pool, the request is retried once against this alternate origin. */
  adaptiveRouting?: LoadBalancingLoadBalancerComponentsSchemasSingleResponse_AdaptiveRouting | Computed<LoadBalancingLoadBalancerComponentsSchemasSingleResponse_AdaptiveRouting>;
  /** A mapping of country codes to a list of pool IDs (ordered by their failover priority) for the given country. Any country not explicitly defined will fall back to using the corresponding region_pool mapping if it exists else to default_pools. */
  countryPools?: Record<string, string[]> | Computed<Record<string, string[]>>;
  /** A list of pool IDs ordered by their failover priority. Pools defined here are used by default, or when region_pools are not configured for a given region. */
  defaultPools: string[] | Computed<string[]>;
  /** Object description. */
  description?: string | Computed<string>;
  /** Whether to enable (the default) this load balancer. */
  enabled?: boolean | Computed<boolean>;
  /** The pool ID to use when all other pools are detected as unhealthy. */
  fallbackPool: string | Computed<string>;
  /** Controls location-based steering for non-proxied requests. See `steering_policy` to learn how steering is affected. */
  locationStrategy?: LoadBalancingLoadBalancerComponentsSchemasSingleResponse_LocationStrategy | Computed<LoadBalancingLoadBalancerComponentsSchemasSingleResponse_LocationStrategy>;
  /** The DNS hostname to associate with your Load Balancer. If this hostname already exists as a DNS record in Cloudflare's DNS, the Load Balancer will take precedence and the DNS record will not be used. */
  name: string | Computed<string>;
  /** List of networks where Load Balancer or Pool is enabled. */
  networks?: string[] | Computed<string[]>;
  /** Enterprise only: A mapping of Cloudflare PoP identifiers to a list of pool IDs (ordered by their failover priority) for the PoP (datacenter). Any PoPs not explicitly defined will fall back to using the corresponding country_pool, then region_pool mapping if it exists else to default_pools. */
  popPools?: Record<string, string[]> | Computed<Record<string, string[]>>;
  /** Whether the hostname should be gray clouded (false) or orange clouded (true). */
  proxied?: boolean | Computed<boolean>;
  /** Configures pool weights. - `steering_policy="random"`: A random pool is selected with probability proportional to pool weights. - `steering_policy="least_outstanding_requests"`: Use pool weights to scale each pool's outstanding requests. - `steering_policy="least_connections"`: Use pool weights to scale each pool's open connections. */
  randomSteering?: LoadBalancingLoadBalancerComponentsSchemasSingleResponse_RandomSteering | Computed<LoadBalancingLoadBalancerComponentsSchemasSingleResponse_RandomSteering>;
  /** A mapping of region codes to a list of pool IDs (ordered by their failover priority) for the given region. Any regions not explicitly defined will fall back to using default_pools. */
  regionPools?: Record<string, string[]> | Computed<Record<string, string[]>>;
  /** BETA Field Not General Access: A list of rules for this load balancer to execute. */
  rules?: LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result_Rules[] | Computed<LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result_Rules[]>;
  /** Specifies the type of session affinity the load balancer should use unless specified as `"none"`. The supported types are: - `"cookie"`: On the first request to a proxied load balancer, a cookie is generated, encoding information of which origin the request will be forwarded to. Subsequent requests, by the same client to the same load balancer, will be sent to the origin server the cookie encodes, for the duration of the cookie and as long as the origin server remains healthy. If the cookie has expired or the origin server is unhealthy, then a new origin server is calculated and used. - `"ip_cookie"`: Behaves the same as `"cookie"` except the initial origin selection is stable and based on the client's ip address. - `"header"`: On the first request to a proxied load balancer, a session key based on the configured HTTP headers (see `session_affinity_attributes.headers`) is generated, encoding the request headers used for storing in the load balancer session state which origin the request will be forwarded to. Subsequent requests to the load balancer with the same headers will be sent to the same origin server, for the duration of the session and as long as the origin server remains healthy. If the session has been idle for the duration of `session_affinity_ttl` seconds or the origin server is unhealthy, then a new origin server is calculated and used. See `headers` in `session_affinity_attributes` for additional required configuration. */
  sessionAffinity?: string | Computed<string>;
  /** Configures attributes for session affinity. */
  sessionAffinityAttributes?: LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result_Rules_Overrides_SessionAffinityAttributes | Computed<LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result_Rules_Overrides_SessionAffinityAttributes>;
  /** Time, in seconds, until a client's session expires after being created. Once the expiry time has been reached, subsequent requests may get sent to a different origin server. The accepted ranges per `session_affinity` policy are: - `"cookie"` / `"ip_cookie"`: The current default of 23 hours will be used unless explicitly set. The accepted range of values is between [1800, 604800]. - `"header"`: The current default of 1800 seconds will be used unless explicitly set. The accepted range of values is between [30, 3600]. Note: With session affinity by header, sessions only expire after they haven't been used for the number of seconds specified. */
  sessionAffinityTtl?: number | Computed<number>;
  /** Steering Policy for this load balancer. - `"off"`: Use `default_pools`. - `"geo"`: Use `region_pools`/`country_pools`/`pop_pools`. For non-proxied requests, the country for `country_pools` is determined by `location_strategy`. - `"random"`: Select a pool randomly. - `"dynamic_latency"`: Use round trip time to select the closest pool in default_pools (requires pool health checks). - `"proximity"`: Use the pools' latitude and longitude to select the closest pool using the Cloudflare PoP location for proxied requests or the location determined by `location_strategy` for non-proxied requests. - `"least_outstanding_requests"`: Select a pool by taking into consideration `random_steering` weights, as well as each pool's number of outstanding requests. Pools with more pending requests are weighted proportionately less relative to others. - `"least_connections"`: Select a pool by taking into consideration `random_steering` weights, as well as each pool's number of open connections. Pools with more open connections are weighted proportionately less relative to others. Supported for HTTP/1 and HTTP/2 connections. - `""`: Will map to `"geo"` if you use `region_pools`/`country_pools`/`pop_pools` otherwise `"off"`. */
  steeringPolicy?: string | Computed<string>;
  /** Time to live (TTL) of the DNS entry for the IP address returned by this load balancer. This only applies to gray-clouded (unproxied) load balancers. */
  ttl?: number | Computed<number>;
  /** path parameter, not part of the API's own resource representation */
  accountId: string | Computed<string>;
  /** path parameter, not part of the API's own resource representation */
  loadBalancerId: string | Computed<string>;
}

export interface LoadBalancingLoadBalancerComponentsSchemasSingleResponseAttrs {
  /** Controls features that modify the routing of requests to pools and origins in response to dynamic conditions, such as during the interval between active health monitoring requests. For example, zero-downtime failover occurs immediately when an origin becomes unavailable due to HTTP 521, 522, or 523 response codes. If there is another healthy origin in the same pool, the request is retried once against this alternate origin. */
  adaptiveRouting: LoadBalancingLoadBalancerComponentsSchemasSingleResponse_AdaptiveRouting;
  /** A mapping of country codes to a list of pool IDs (ordered by their failover priority) for the given country. Any country not explicitly defined will fall back to using the corresponding region_pool mapping if it exists else to default_pools. */
  countryPools: Record<string, string[]>;
  /** A list of pool IDs ordered by their failover priority. Pools defined here are used by default, or when region_pools are not configured for a given region. */
  defaultPools: string[];
  /** Object description. */
  description: string;
  /** Whether to enable (the default) this load balancer. */
  enabled: boolean;
  /** The pool ID to use when all other pools are detected as unhealthy. */
  fallbackPool: string;
  /** Controls location-based steering for non-proxied requests. See `steering_policy` to learn how steering is affected. */
  locationStrategy: LoadBalancingLoadBalancerComponentsSchemasSingleResponse_LocationStrategy;
  /** The DNS hostname to associate with your Load Balancer. If this hostname already exists as a DNS record in Cloudflare's DNS, the Load Balancer will take precedence and the DNS record will not be used. */
  name: string;
  /** List of networks where Load Balancer or Pool is enabled. */
  networks: string[];
  /** Enterprise only: A mapping of Cloudflare PoP identifiers to a list of pool IDs (ordered by their failover priority) for the PoP (datacenter). Any PoPs not explicitly defined will fall back to using the corresponding country_pool, then region_pool mapping if it exists else to default_pools. */
  popPools: Record<string, string[]>;
  /** Whether the hostname should be gray clouded (false) or orange clouded (true). */
  proxied: boolean;
  /** Configures pool weights. - `steering_policy="random"`: A random pool is selected with probability proportional to pool weights. - `steering_policy="least_outstanding_requests"`: Use pool weights to scale each pool's outstanding requests. - `steering_policy="least_connections"`: Use pool weights to scale each pool's open connections. */
  randomSteering: LoadBalancingLoadBalancerComponentsSchemasSingleResponse_RandomSteering;
  /** A mapping of region codes to a list of pool IDs (ordered by their failover priority) for the given region. Any regions not explicitly defined will fall back to using default_pools. */
  regionPools: Record<string, string[]>;
  result: LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result;
  /** BETA Field Not General Access: A list of rules for this load balancer to execute. */
  rules: LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result_Rules[];
  /** Specifies the type of session affinity the load balancer should use unless specified as `"none"`. The supported types are: - `"cookie"`: On the first request to a proxied load balancer, a cookie is generated, encoding information of which origin the request will be forwarded to. Subsequent requests, by the same client to the same load balancer, will be sent to the origin server the cookie encodes, for the duration of the cookie and as long as the origin server remains healthy. If the cookie has expired or the origin server is unhealthy, then a new origin server is calculated and used. - `"ip_cookie"`: Behaves the same as `"cookie"` except the initial origin selection is stable and based on the client's ip address. - `"header"`: On the first request to a proxied load balancer, a session key based on the configured HTTP headers (see `session_affinity_attributes.headers`) is generated, encoding the request headers used for storing in the load balancer session state which origin the request will be forwarded to. Subsequent requests to the load balancer with the same headers will be sent to the same origin server, for the duration of the session and as long as the origin server remains healthy. If the session has been idle for the duration of `session_affinity_ttl` seconds or the origin server is unhealthy, then a new origin server is calculated and used. See `headers` in `session_affinity_attributes` for additional required configuration. */
  sessionAffinity: string;
  /** Configures attributes for session affinity. */
  sessionAffinityAttributes: LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result_Rules_Overrides_SessionAffinityAttributes;
  /** Time, in seconds, until a client's session expires after being created. Once the expiry time has been reached, subsequent requests may get sent to a different origin server. The accepted ranges per `session_affinity` policy are: - `"cookie"` / `"ip_cookie"`: The current default of 23 hours will be used unless explicitly set. The accepted range of values is between [1800, 604800]. - `"header"`: The current default of 1800 seconds will be used unless explicitly set. The accepted range of values is between [30, 3600]. Note: With session affinity by header, sessions only expire after they haven't been used for the number of seconds specified. */
  sessionAffinityTtl: number;
  /** Steering Policy for this load balancer. - `"off"`: Use `default_pools`. - `"geo"`: Use `region_pools`/`country_pools`/`pop_pools`. For non-proxied requests, the country for `country_pools` is determined by `location_strategy`. - `"random"`: Select a pool randomly. - `"dynamic_latency"`: Use round trip time to select the closest pool in default_pools (requires pool health checks). - `"proximity"`: Use the pools' latitude and longitude to select the closest pool using the Cloudflare PoP location for proxied requests or the location determined by `location_strategy` for non-proxied requests. - `"least_outstanding_requests"`: Select a pool by taking into consideration `random_steering` weights, as well as each pool's number of outstanding requests. Pools with more pending requests are weighted proportionately less relative to others. - `"least_connections"`: Select a pool by taking into consideration `random_steering` weights, as well as each pool's number of open connections. Pools with more open connections are weighted proportionately less relative to others. Supported for HTTP/1 and HTTP/2 connections. - `""`: Will map to `"geo"` if you use `region_pools`/`country_pools`/`pop_pools` otherwise `"off"`. */
  steeringPolicy: string;
  /** Time to live (TTL) of the DNS entry for the IP address returned by this load balancer. This only applies to gray-clouded (unproxied) load balancers. */
  ttl: number;
  /** path parameter, not part of the API's own resource representation */
  accountId: string;
  /** path parameter, not part of the API's own resource representation */
  loadBalancerId: string;
}

export const LoadBalancingLoadBalancerComponentsSchemasSingleResponse: ResourceBinding<LoadBalancingLoadBalancerComponentsSchemasSingleResponseConfig, LoadBalancingLoadBalancerComponentsSchemasSingleResponseAttrs> = {
  wireType: "cloudflare_load_balancing_load_balancer_components_schemas_single_response",
  fields: {
    adaptiveRouting: {
      wireName: "adaptive_routing",
      kind: "object",
      fields: LoadBalancingLoadBalancerComponentsSchemasSingleResponse_AdaptiveRoutingFields,
    },
    countryPools: "country_pools",
    defaultPools: "default_pools",
    description: "description",
    enabled: "enabled",
    fallbackPool: "fallback_pool",
    locationStrategy: {
      wireName: "location_strategy",
      kind: "object",
      fields: LoadBalancingLoadBalancerComponentsSchemasSingleResponse_LocationStrategyFields,
    },
    name: "name",
    networks: "networks",
    popPools: "pop_pools",
    proxied: "proxied",
    randomSteering: {
      wireName: "random_steering",
      kind: "object",
      fields: LoadBalancingLoadBalancerComponentsSchemasSingleResponse_RandomSteeringFields,
    },
    regionPools: "region_pools",
    rules: {
      wireName: "rules",
      kind: "list",
      fields: LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result_RulesFields,
    },
    sessionAffinity: "session_affinity",
    sessionAffinityAttributes: {
      wireName: "session_affinity_attributes",
      kind: "object",
      fields: LoadBalancingLoadBalancerComponentsSchemasSingleResponse_Result_Rules_Overrides_SessionAffinityAttributesFields,
    },
    sessionAffinityTtl: "session_affinity_ttl",
    steeringPolicy: "steering_policy",
    ttl: "ttl",
    accountId: "account_id",
    loadBalancerId: "load_balancer_id",
  },
};

// Code generated by `ubx sdk gen` from cloudflare_stream_video_response_single. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface StreamVideoResponseSingle_Watermark {
  /** The unique identifier for the watermark profile. */
  uid?: string | Computed<string>;
}

const StreamVideoResponseSingle_WatermarkFields: FieldMap = {
  uid: "uid",
};

export interface StreamVideoResponseSingleConfig {
  /** Lists the origins allowed to display the video. Enter allowed origin domains in an array and use `*` for wildcard subdomains. Empty arrays allow the video to be viewed on any origin. */
  allowedOrigins?: string[] | Computed<string[]>;
  /** A user-defined identifier for the media creator. */
  creator?: string | Computed<string>;
  /** A video's URL. The server must be publicly routable and support `HTTP HEAD` requests and `HTTP GET` range requests. The server should respond to `HTTP HEAD` requests with a `content-range` header that includes the size of the file. This is the preferred field over `url`. */
  input?: string | Computed<string>;
  /** A user modifiable key-value store used to reference other systems of record for managing videos. */
  meta?: unknown | Computed<unknown>;
  /** A video's name. Used for legacy compatibility. */
  name?: string | Computed<string>;
  /** Indicates whether the video can be a accessed using the UID. When set to `true`, a signed token must be generated with a signing key to view the video. */
  requireSignedUrls?: boolean | Computed<boolean>;
  /** Indicates the date and time at which the video will be deleted. Omit the field to indicate no change, or include with a `null` value to remove an existing scheduled deletion. If specified, must be at least 30 days from upload time. */
  scheduledDeletion?: string | Computed<string>;
  /** The timestamp for a thumbnail image calculated as a percentage value of the video's duration. To convert from a second-wise timestamp to a percentage, divide the desired timestamp by the total duration of the video. If this value is not set, the default thumbnail image is taken from 0s of the video. */
  thumbnailTimestampPct?: number | Computed<number>;
  /** A video's URL. The server must be publicly routable and support `HTTP HEAD` requests and `HTTP GET` range requests. The server should respond to `HTTP HEAD` requests with a `content-range` header that includes the size of the file. This field is deprecated in favor of `input`. */
  url?: string | Computed<string>;
  watermark?: StreamVideoResponseSingle_Watermark | Computed<StreamVideoResponseSingle_Watermark>;
  /** path parameter, not part of the API's own resource representation */
  accountId: string | Computed<string>;
  /** path parameter, not part of the API's own resource representation */
  identifier: string | Computed<string>;
}

export interface StreamVideoResponseSingleAttrs {
  /** Lists the origins allowed to display the video. Enter allowed origin domains in an array and use `*` for wildcard subdomains. Empty arrays allow the video to be viewed on any origin. */
  allowedOrigins: string[];
  /** A user-defined identifier for the media creator. */
  creator: string;
  /** A video's URL. The server must be publicly routable and support `HTTP HEAD` requests and `HTTP GET` range requests. The server should respond to `HTTP HEAD` requests with a `content-range` header that includes the size of the file. This is the preferred field over `url`. */
  input: string;
  /** A user modifiable key-value store used to reference other systems of record for managing videos. */
  meta: unknown;
  /** A video's name. Used for legacy compatibility. */
  name: string;
  /** Indicates whether the video can be a accessed using the UID. When set to `true`, a signed token must be generated with a signing key to view the video. */
  requireSignedUrls: boolean;
  /** Indicates the date and time at which the video will be deleted. Omit the field to indicate no change, or include with a `null` value to remove an existing scheduled deletion. If specified, must be at least 30 days from upload time. */
  scheduledDeletion: string;
  /** The timestamp for a thumbnail image calculated as a percentage value of the video's duration. To convert from a second-wise timestamp to a percentage, divide the desired timestamp by the total duration of the video. If this value is not set, the default thumbnail image is taken from 0s of the video. */
  thumbnailTimestampPct: number;
  /** A video's URL. The server must be publicly routable and support `HTTP HEAD` requests and `HTTP GET` range requests. The server should respond to `HTTP HEAD` requests with a `content-range` header that includes the size of the file. This field is deprecated in favor of `input`. */
  url: string;
  watermark: StreamVideoResponseSingle_Watermark;
  /** path parameter, not part of the API's own resource representation */
  accountId: string;
  /** path parameter, not part of the API's own resource representation */
  identifier: string;
}

export const StreamVideoResponseSingle: ResourceBinding<StreamVideoResponseSingleConfig, StreamVideoResponseSingleAttrs> = {
  wireType: "cloudflare_stream_video_response_single",
  fields: {
    allowedOrigins: "allowed_origins",
    creator: "creator",
    input: "input",
    meta: "meta",
    name: "name",
    requireSignedUrls: "require_signed_urls",
    scheduledDeletion: "scheduled_deletion",
    thumbnailTimestampPct: "thumbnail_timestamp_pct",
    url: "url",
    watermark: {
      wireName: "watermark",
      kind: "object",
      fields: StreamVideoResponseSingle_WatermarkFields,
    },
    accountId: "account_id",
    identifier: "identifier",
  },
};

// Code generated by `ubx sdk gen` from cloudflare_registrar_api_sandbox_registration_response_single. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface RegistrarApiSandboxRegistrationResponseSingle_Contacts_Administrator_PostalInfo_Address {
  city?: string | Computed<string>;
  countryCode?: string | Computed<string>;
  postalCode?: string | Computed<string>;
  state?: string | Computed<string>;
  street?: string | Computed<string>;
}

export interface RegistrarApiSandboxRegistrationResponseSingle_Contacts_Administrator_PostalInfo {
  address?: RegistrarApiSandboxRegistrationResponseSingle_Contacts_Administrator_PostalInfo_Address | Computed<RegistrarApiSandboxRegistrationResponseSingle_Contacts_Administrator_PostalInfo_Address>;
  name?: string | Computed<string>;
  organization?: string | Computed<string>;
}

export interface RegistrarApiSandboxRegistrationResponseSingle_Contacts_Administrator {
  email?: string | Computed<string>;
  fax?: string | Computed<string>;
  phone?: string | Computed<string>;
  postalInfo?: RegistrarApiSandboxRegistrationResponseSingle_Contacts_Administrator_PostalInfo | Computed<RegistrarApiSandboxRegistrationResponseSingle_Contacts_Administrator_PostalInfo>;
}

export interface RegistrarApiSandboxRegistrationResponseSingle_Contacts {
  /** Optional administrator contact. Accepted only when the extension schema includes this role. When the registry requires an omitted contact, Cloudflare may derive it from `contacts.registrant`. */
  administrator?: RegistrarApiSandboxRegistrationResponseSingle_Contacts_Administrator | Computed<RegistrarApiSandboxRegistrationResponseSingle_Contacts_Administrator>;
  /** Optional billing contact. Accepted only when the extension schema includes this role. When the registry requires an omitted contact, Cloudflare may derive it from `contacts.registrant`. */
  billing?: RegistrarApiSandboxRegistrationResponseSingle_Contacts_Administrator | Computed<RegistrarApiSandboxRegistrationResponseSingle_Contacts_Administrator>;
  /** Optional registrant contact. If omitted, the account's default address book entry is used instead. */
  registrant?: RegistrarApiSandboxRegistrationResponseSingle_Contacts_Administrator | Computed<RegistrarApiSandboxRegistrationResponseSingle_Contacts_Administrator>;
  /** Optional technical contact. Accepted only when the extension schema includes this role. When the registry requires an omitted contact, Cloudflare may derive it from `contacts.registrant`. */
  technical?: RegistrarApiSandboxRegistrationResponseSingle_Contacts_Administrator | Computed<RegistrarApiSandboxRegistrationResponseSingle_Contacts_Administrator>;
}

const RegistrarApiSandboxRegistrationResponseSingle_Contacts_Administrator_PostalInfo_AddressFields: FieldMap = {
  city: "city",
  countryCode: "country_code",
  postalCode: "postal_code",
  state: "state",
  street: "street",
};

const RegistrarApiSandboxRegistrationResponseSingle_Contacts_Administrator_PostalInfoFields: FieldMap = {
  address: {
    wireName: "address",
    kind: "object",
    fields: RegistrarApiSandboxRegistrationResponseSingle_Contacts_Administrator_PostalInfo_AddressFields,
  },
  name: "name",
  organization: "organization",
};

const RegistrarApiSandboxRegistrationResponseSingle_Contacts_AdministratorFields: FieldMap = {
  email: "email",
  fax: "fax",
  phone: "phone",
  postalInfo: {
    wireName: "postal_info",
    kind: "object",
    fields: RegistrarApiSandboxRegistrationResponseSingle_Contacts_Administrator_PostalInfoFields,
  },
};

const RegistrarApiSandboxRegistrationResponseSingle_ContactsFields: FieldMap = {
  administrator: {
    wireName: "administrator",
    kind: "object",
    fields: RegistrarApiSandboxRegistrationResponseSingle_Contacts_AdministratorFields,
  },
  billing: {
    wireName: "billing",
    kind: "object",
    fields: RegistrarApiSandboxRegistrationResponseSingle_Contacts_AdministratorFields,
  },
  registrant: {
    wireName: "registrant",
    kind: "object",
    fields: RegistrarApiSandboxRegistrationResponseSingle_Contacts_AdministratorFields,
  },
  technical: {
    wireName: "technical",
    kind: "object",
    fields: RegistrarApiSandboxRegistrationResponseSingle_Contacts_AdministratorFields,
  },
};

export interface RegistrarApiSandboxRegistrationResponseSingleConfig {
  /** Provides user acknowledgements for a specific extension or premium registration flow. The extension registration schema from the extension discovery endpoint identifies the required keys. */
  acknowledgements?: unknown | Computed<unknown>;
  /** Enable or disable automatic renewal. Defaults to `false` if omitted. Setting this field to `true` is an explicit opt-in authorizing Cloudflare to charge the account's default payment method up to 30 days before domain expiry to renew the domain automatically. Renewal pricing may change over time based on registry pricing. */
  autoRenew?: boolean | Computed<boolean>;
  /** Provides registry-specific contact extension values for the registrant. `GET /accounts/{account_id}/registrar/extensions/{extension}` identifies the required keys and allowed values for each extension in the `registration_schema.properties.contact_extensions` object. Examples include `.us` nexus fields, `.uk` registrant type fields, and `.ca` legal type fields. Omit this object when the extension's registration schema excludes `contact_extensions`. */
  contactExtensions?: unknown | Computed<unknown>;
  /** Provides contact data for the registration request. The per-extension schema from `GET /accounts/{account_id}/registrar/extensions/{extension}` defines the accepted contact roles. Every currently supported extension requires only `contacts.registrant` from API callers. Callers may provide additional roles such as `technical`, `administrator`, and `billing` when the extension schema includes them. When a registry requires an omitted role, Cloudflare may derive that contact from `contacts.registrant`. When the request omits either the entire `contacts` object or `contacts.registrant`, the system uses the account's default address book entry as the registrant contact. The account owner must configure this default at `https://dash.cloudflare.com/{account_id}/domains/registrations`, where they can create or update the address book entry and accept the required agreement. Dashboard settings currently provide the only way to manage address book entries. Without either a default address book entry or a registrant contact, the registration request fails validation. */
  contacts?: RegistrarApiSandboxRegistrationResponseSingle_Contacts | Computed<RegistrarApiSandboxRegistrationResponseSingle_Contacts>;
  /** Provides a fully qualified domain name (FQDN), including the extension (e.g., `example.com`, `mybrand.app`). The domain name uniquely identifies a registration. Cloudflare permits only one registration per domain, making the domain name a natural idempotency key for registration requests. */
  domainName: string | Computed<string>;
  /** Sets the WHOIS privacy mode for the registration. Defaults to `redaction`. - `off`: Disables WHOIS privacy. - `redaction`: Requests WHOIS redaction where the extension supports it. Some extensions exclude privacy and redaction. */
  privacyMode?: string | Computed<string>;
  /** Sets the registration term from 1 to 10 years. When omitted, this field defaults to the registry's minimum registration period for the extension. Most extensions require 1 year, while some require longer minimum terms (e.g., `.ai` requires 2 years). Each registry may also enforce its own maximum registration term. A request above that maximum fails. When uncertain, omit this field to use the default. */
  years?: number | Computed<number>;
  /** path parameter, not part of the API's own resource representation */
  accountId: string | Computed<string>;
}

export interface RegistrarApiSandboxRegistrationResponseSingleAttrs {
  /** Provides user acknowledgements for a specific extension or premium registration flow. The extension registration schema from the extension discovery endpoint identifies the required keys. */
  acknowledgements: unknown;
  /** Enable or disable automatic renewal. Defaults to `false` if omitted. Setting this field to `true` is an explicit opt-in authorizing Cloudflare to charge the account's default payment method up to 30 days before domain expiry to renew the domain automatically. Renewal pricing may change over time based on registry pricing. */
  autoRenew: boolean;
  /** Provides registry-specific contact extension values for the registrant. `GET /accounts/{account_id}/registrar/extensions/{extension}` identifies the required keys and allowed values for each extension in the `registration_schema.properties.contact_extensions` object. Examples include `.us` nexus fields, `.uk` registrant type fields, and `.ca` legal type fields. Omit this object when the extension's registration schema excludes `contact_extensions`. */
  contactExtensions: unknown;
  /** Provides contact data for the registration request. The per-extension schema from `GET /accounts/{account_id}/registrar/extensions/{extension}` defines the accepted contact roles. Every currently supported extension requires only `contacts.registrant` from API callers. Callers may provide additional roles such as `technical`, `administrator`, and `billing` when the extension schema includes them. When a registry requires an omitted role, Cloudflare may derive that contact from `contacts.registrant`. When the request omits either the entire `contacts` object or `contacts.registrant`, the system uses the account's default address book entry as the registrant contact. The account owner must configure this default at `https://dash.cloudflare.com/{account_id}/domains/registrations`, where they can create or update the address book entry and accept the required agreement. Dashboard settings currently provide the only way to manage address book entries. Without either a default address book entry or a registrant contact, the registration request fails validation. */
  contacts: RegistrarApiSandboxRegistrationResponseSingle_Contacts;
  /** Provides a fully qualified domain name (FQDN), including the extension (e.g., `example.com`, `mybrand.app`). The domain name uniquely identifies a registration. Cloudflare permits only one registration per domain, making the domain name a natural idempotency key for registration requests. */
  domainName: string;
  /** Sets the WHOIS privacy mode for the registration. Defaults to `redaction`. - `off`: Disables WHOIS privacy. - `redaction`: Requests WHOIS redaction where the extension supports it. Some extensions exclude privacy and redaction. */
  privacyMode: string;
  /** Sets the registration term from 1 to 10 years. When omitted, this field defaults to the registry's minimum registration period for the extension. Most extensions require 1 year, while some require longer minimum terms (e.g., `.ai` requires 2 years). Each registry may also enforce its own maximum registration term. A request above that maximum fails. When uncertain, omit this field to use the default. */
  years: number;
  /** path parameter, not part of the API's own resource representation */
  accountId: string;
}

export const RegistrarApiSandboxRegistrationResponseSingle: ResourceBinding<RegistrarApiSandboxRegistrationResponseSingleConfig, RegistrarApiSandboxRegistrationResponseSingleAttrs> = {
  wireType: "cloudflare_registrar_api_sandbox_registration_response_single",
  fields: {
    acknowledgements: "acknowledgements",
    autoRenew: "auto_renew",
    contactExtensions: "contact_extensions",
    contacts: {
      wireName: "contacts",
      kind: "object",
      fields: RegistrarApiSandboxRegistrationResponseSingle_ContactsFields,
    },
    domainName: "domain_name",
    privacyMode: "privacy_mode",
    years: "years",
    accountId: "account_id",
  },
};

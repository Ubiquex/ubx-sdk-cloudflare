// Code generated by `ubx sdk gen` from cloudflare_policy. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface Policy_Actions_RemediationTypes {
  remediationTypeId?: string | Computed<string>;
}

export interface Policy_Actions_WebhookConfigs {
  webhookConfigId?: string | Computed<string>;
}

export interface Policy_Actions {
  /** Remediation actions to execute (at most one). */
  remediationTypes?: Policy_Actions_RemediationTypes[] | Computed<Policy_Actions_RemediationTypes[]>;
  /** Webhook actions to execute. */
  webhookConfigs?: Policy_Actions_WebhookConfigs[] | Computed<Policy_Actions_WebhookConfigs[]>;
}

const Policy_Actions_RemediationTypesFields: FieldMap = {
  remediationTypeId: "remediation_type_id",
};

const Policy_Actions_WebhookConfigsFields: FieldMap = {
  webhookConfigId: "webhook_config_id",
};

const Policy_ActionsFields: FieldMap = {
  remediationTypes: {
    wireName: "remediation_types",
    kind: "list",
    fields: Policy_Actions_RemediationTypesFields,
  },
  webhookConfigs: {
    wireName: "webhook_configs",
    kind: "list",
    fields: Policy_Actions_WebhookConfigsFields,
  },
};

export interface PolicyConfig {
  /** Actions to execute when this policy is triggered, grouped by action type. A policy must contain at least one action across all groups and may include at most one remediation. */
  actions: Policy_Actions | Computed<Policy_Actions>;
  /** When true, the policy applies to all integrations for the account. When false, integration_ids must be provided. */
  appliesToAllIntegrations: boolean | Computed<boolean>;
  /** Optional description of what this policy does. */
  description?: string | Computed<string>;
  /** Display name for the policy configuration. */
  displayName: string | Computed<string>;
  /** Boolean specifying if the policy is enabled or disabled. */
  enabled: boolean | Computed<boolean>;
  /** The finding type this policy is associated with. All remediation actions must match this finding type. */
  findingTypeId: string | Computed<string>;
  /** The integrations this policy applies to. Required when applies_to_all_integrations is false. */
  integrationIds?: string[] | Computed<string[]>;
  /** path parameter, not part of the API's own resource representation */
  accountId: string | Computed<string>;
  /** path parameter, not part of the API's own resource representation */
  policyId: string | Computed<string>;
}

export interface PolicyAttrs {
  /** Actions to execute when this policy is triggered, grouped by action type. A policy must contain at least one action across all groups and may include at most one remediation. */
  actions: Policy_Actions;
  /** When true, the policy applies to all integrations for the account. When false, integration_ids must be provided. */
  appliesToAllIntegrations: boolean;
  /** Optional description of what this policy does. */
  description: string;
  /** Display name for the policy configuration. */
  displayName: string;
  /** Boolean specifying if the policy is enabled or disabled. */
  enabled: boolean;
  /** The finding type this policy is associated with. All remediation actions must match this finding type. */
  findingTypeId: string;
  /** The integrations this policy applies to. Required when applies_to_all_integrations is false. */
  integrationIds: string[];
  /** path parameter, not part of the API's own resource representation */
  accountId: string;
  /** path parameter, not part of the API's own resource representation */
  policyId: string;
}

export const Policy: ResourceBinding<PolicyConfig, PolicyAttrs> = {
  wireType: "cloudflare_policy",
  fields: {
    actions: {
      wireName: "actions",
      kind: "object",
      fields: Policy_ActionsFields,
    },
    appliesToAllIntegrations: "applies_to_all_integrations",
    description: "description",
    displayName: "display_name",
    enabled: "enabled",
    findingTypeId: "finding_type_id",
    integrationIds: "integration_ids",
    accountId: "account_id",
    policyId: "policy_id",
  },
};

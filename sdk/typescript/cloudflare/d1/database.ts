// Code generated by `ubx sdk gen` from cloudflare_database. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface Database_ReadReplication {
  /** The read replication mode for the database. Use 'auto' to create replicas and allow D1 automatically place them around the world, or 'disabled' to not use any database replicas (it can take a few hours for all replicas to be deleted). */
  mode: string | Computed<string>;
}

const Database_ReadReplicationFields: FieldMap = {
  mode: "mode",
};

export interface DatabaseConfig {
  /** Specify the location to restrict the D1 database to run and store data. If this option is present, the location hint is ignored. */
  jurisdiction?: string | Computed<string>;
  /** D1 database name. */
  name: string | Computed<string>;
  /** Specify the region to create the D1 primary, if available. If this option is omitted, the D1 will be created as close as possible to the current user. */
  primaryLocationHint?: string | Computed<string>;
  /** Configuration for D1 read replication. */
  readReplication?: Database_ReadReplication | Computed<Database_ReadReplication>;
  /** path parameter, not part of the API's own resource representation */
  accountId: string | Computed<string>;
  /** path parameter, not part of the API's own resource representation */
  databaseId: string | Computed<string>;
}

export interface DatabaseAttrs {
  /** Specify the location to restrict the D1 database to run and store data. If this option is present, the location hint is ignored. */
  jurisdiction: string;
  /** D1 database name. */
  name: string;
  /** Specify the region to create the D1 primary, if available. If this option is omitted, the D1 will be created as close as possible to the current user. */
  primaryLocationHint: string;
  /** Configuration for D1 read replication. */
  readReplication: Database_ReadReplication;
  /** path parameter, not part of the API's own resource representation */
  accountId: string;
  /** path parameter, not part of the API's own resource representation */
  databaseId: string;
}

export const Database: ResourceBinding<DatabaseConfig, DatabaseAttrs> = {
  wireType: "cloudflare_database",
  fields: {
    jurisdiction: "jurisdiction",
    name: "name",
    primaryLocationHint: "primary_location_hint",
    readReplication: {
      wireName: "read_replication",
      kind: "object",
      fields: Database_ReadReplicationFields,
    },
    accountId: "account_id",
    databaseId: "database_id",
  },
};

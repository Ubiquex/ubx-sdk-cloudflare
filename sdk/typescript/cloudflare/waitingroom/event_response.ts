// Code generated by `ubx sdk gen` from cloudflare_waitingroom_event_response. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface EventResponse_Result {
  createdOn?: string | Computed<string>;
  /** If set, the event will override the waiting room's `custom_page_html` property while it is active. If null, the event will inherit it. */
  customPageHtml?: string | Computed<string>;
  /** A note that you can use to add more details about the event. */
  description?: string | Computed<string>;
  /** If set, the event will override the waiting room's `disable_session_renewal` property while it is active. If null, the event will inherit it. */
  disableSessionRenewal?: boolean | Computed<boolean>;
  /** An ISO 8601 timestamp that marks the end of the event. */
  eventEndTime?: string | Computed<string>;
  /** An ISO 8601 timestamp that marks the start of the event. At this time, queued users will be processed with the event's configuration. The start time must be at least one minute before `event_end_time`. */
  eventStartTime?: string | Computed<string>;
  id?: string | Computed<string>;
  modifiedOn?: string | Computed<string>;
  /** A unique name to identify the event. Only alphanumeric characters, hyphens and underscores are allowed. */
  name?: string | Computed<string>;
  /** If set, the event will override the waiting room's `new_users_per_minute` property while it is active. If null, the event will inherit it. This can only be set if the event's `total_active_users` property is also set. */
  newUsersPerMinute?: number | Computed<number>;
  /** An ISO 8601 timestamp that marks when to begin queueing all users before the event starts. The prequeue must start at least five minutes before `event_start_time`. */
  prequeueStartTime?: string | Computed<string>;
  /** If set, the event will override the waiting room's `queueing_method` property while it is active. If null, the event will inherit it. */
  queueingMethod?: string | Computed<string>;
  /** If set, the event will override the waiting room's `session_duration` property while it is active. If null, the event will inherit it. */
  sessionDuration?: number | Computed<number>;
  /** If enabled, users in the prequeue will be shuffled randomly at the `event_start_time`. Requires that `prequeue_start_time` is not null. This is useful for situations when many users will join the event prequeue at the same time and you want to shuffle them to ensure fairness. Naturally, it makes the most sense to enable this feature when the `queueing_method` during the event respects ordering such as **fifo**, or else the shuffling may be unnecessary. */
  shuffleAtEventStart?: boolean | Computed<boolean>;
  /** Suspends or allows an event. If set to `true`, the event is ignored and traffic will be handled based on the waiting room configuration. */
  suspended?: boolean | Computed<boolean>;
  /** If set, the event will override the waiting room's `total_active_users` property while it is active. If null, the event will inherit it. This can only be set if the event's `new_users_per_minute` property is also set. */
  totalActiveUsers?: number | Computed<number>;
  /** If set, the event will override the waiting room's `turnstile_action` property while it is active. If null, the event will inherit it. */
  turnstileAction?: string | Computed<string>;
  /** If set, the event will override the waiting room's `turnstile_mode` property while it is active. If null, the event will inherit it. */
  turnstileMode?: string | Computed<string>;
}

export interface EventResponseConfig {
  /** If set, the event will override the waiting room's `custom_page_html` property while it is active. If null, the event will inherit it. */
  customPageHtml?: string | Computed<string>;
  /** A note that you can use to add more details about the event. */
  description?: string | Computed<string>;
  /** If set, the event will override the waiting room's `disable_session_renewal` property while it is active. If null, the event will inherit it. */
  disableSessionRenewal?: boolean | Computed<boolean>;
  /** An ISO 8601 timestamp that marks the end of the event. */
  eventEndTime: string | Computed<string>;
  /** An ISO 8601 timestamp that marks the start of the event. At this time, queued users will be processed with the event's configuration. The start time must be at least one minute before `event_end_time`. */
  eventStartTime: string | Computed<string>;
  /** A unique name to identify the event. Only alphanumeric characters, hyphens and underscores are allowed. */
  name: string | Computed<string>;
  /** If set, the event will override the waiting room's `new_users_per_minute` property while it is active. If null, the event will inherit it. This can only be set if the event's `total_active_users` property is also set. */
  newUsersPerMinute?: number | Computed<number>;
  /** An ISO 8601 timestamp that marks when to begin queueing all users before the event starts. The prequeue must start at least five minutes before `event_start_time`. */
  prequeueStartTime?: string | Computed<string>;
  /** If set, the event will override the waiting room's `queueing_method` property while it is active. If null, the event will inherit it. */
  queueingMethod?: string | Computed<string>;
  /** If set, the event will override the waiting room's `session_duration` property while it is active. If null, the event will inherit it. */
  sessionDuration?: number | Computed<number>;
  /** If enabled, users in the prequeue will be shuffled randomly at the `event_start_time`. Requires that `prequeue_start_time` is not null. This is useful for situations when many users will join the event prequeue at the same time and you want to shuffle them to ensure fairness. Naturally, it makes the most sense to enable this feature when the `queueing_method` during the event respects ordering such as **fifo**, or else the shuffling may be unnecessary. */
  shuffleAtEventStart?: boolean | Computed<boolean>;
  /** Suspends or allows an event. If set to `true`, the event is ignored and traffic will be handled based on the waiting room configuration. */
  suspended?: boolean | Computed<boolean>;
  /** If set, the event will override the waiting room's `total_active_users` property while it is active. If null, the event will inherit it. This can only be set if the event's `new_users_per_minute` property is also set. */
  totalActiveUsers?: number | Computed<number>;
  /** If set, the event will override the waiting room's `turnstile_action` property while it is active. If null, the event will inherit it. */
  turnstileAction?: string | Computed<string>;
  /** If set, the event will override the waiting room's `turnstile_mode` property while it is active. If null, the event will inherit it. */
  turnstileMode?: string | Computed<string>;
  /** path parameter, not part of the API's own resource representation */
  zoneId: string | Computed<string>;
  /** path parameter, not part of the API's own resource representation */
  waitingRoomId: string | Computed<string>;
  /** path parameter, not part of the API's own resource representation */
  eventId: string | Computed<string>;
}

export interface EventResponseAttrs {
  /** If set, the event will override the waiting room's `custom_page_html` property while it is active. If null, the event will inherit it. */
  customPageHtml: string;
  /** A note that you can use to add more details about the event. */
  description: string;
  /** If set, the event will override the waiting room's `disable_session_renewal` property while it is active. If null, the event will inherit it. */
  disableSessionRenewal: boolean;
  /** An ISO 8601 timestamp that marks the end of the event. */
  eventEndTime: string;
  /** An ISO 8601 timestamp that marks the start of the event. At this time, queued users will be processed with the event's configuration. The start time must be at least one minute before `event_end_time`. */
  eventStartTime: string;
  /** A unique name to identify the event. Only alphanumeric characters, hyphens and underscores are allowed. */
  name: string;
  /** If set, the event will override the waiting room's `new_users_per_minute` property while it is active. If null, the event will inherit it. This can only be set if the event's `total_active_users` property is also set. */
  newUsersPerMinute: number;
  /** An ISO 8601 timestamp that marks when to begin queueing all users before the event starts. The prequeue must start at least five minutes before `event_start_time`. */
  prequeueStartTime: string;
  /** If set, the event will override the waiting room's `queueing_method` property while it is active. If null, the event will inherit it. */
  queueingMethod: string;
  result: EventResponse_Result;
  /** If set, the event will override the waiting room's `session_duration` property while it is active. If null, the event will inherit it. */
  sessionDuration: number;
  /** If enabled, users in the prequeue will be shuffled randomly at the `event_start_time`. Requires that `prequeue_start_time` is not null. This is useful for situations when many users will join the event prequeue at the same time and you want to shuffle them to ensure fairness. Naturally, it makes the most sense to enable this feature when the `queueing_method` during the event respects ordering such as **fifo**, or else the shuffling may be unnecessary. */
  shuffleAtEventStart: boolean;
  /** Suspends or allows an event. If set to `true`, the event is ignored and traffic will be handled based on the waiting room configuration. */
  suspended: boolean;
  /** If set, the event will override the waiting room's `total_active_users` property while it is active. If null, the event will inherit it. This can only be set if the event's `new_users_per_minute` property is also set. */
  totalActiveUsers: number;
  /** If set, the event will override the waiting room's `turnstile_action` property while it is active. If null, the event will inherit it. */
  turnstileAction: string;
  /** If set, the event will override the waiting room's `turnstile_mode` property while it is active. If null, the event will inherit it. */
  turnstileMode: string;
  /** path parameter, not part of the API's own resource representation */
  zoneId: string;
  /** path parameter, not part of the API's own resource representation */
  waitingRoomId: string;
  /** path parameter, not part of the API's own resource representation */
  eventId: string;
}

export const EventResponse: ResourceBinding<EventResponseConfig, EventResponseAttrs> = {
  wireType: "cloudflare_waitingroom_event_response",
  fields: {
    customPageHtml: "custom_page_html",
    description: "description",
    disableSessionRenewal: "disable_session_renewal",
    eventEndTime: "event_end_time",
    eventStartTime: "event_start_time",
    name: "name",
    newUsersPerMinute: "new_users_per_minute",
    prequeueStartTime: "prequeue_start_time",
    queueingMethod: "queueing_method",
    sessionDuration: "session_duration",
    shuffleAtEventStart: "shuffle_at_event_start",
    suspended: "suspended",
    totalActiveUsers: "total_active_users",
    turnstileAction: "turnstile_action",
    turnstileMode: "turnstile_mode",
    zoneId: "zone_id",
    waitingRoomId: "waiting_room_id",
    eventId: "event_id",
  },
};

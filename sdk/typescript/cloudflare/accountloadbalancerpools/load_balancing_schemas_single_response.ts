// Code generated by `ubx sdk gen` from cloudflare_load_balancing_schemas_single_response. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface LoadBalancingSchemasSingleResponse_LoadShedding {
  /** The percent of traffic to shed from the pool, according to the default policy. Applies to new sessions and traffic without session affinity. */
  defaultPercent?: number | Computed<number>;
  /** The default policy to use when load shedding. A random policy randomly sheds a given percent of requests. A hash policy computes a hash over the CF-Connecting-IP address and sheds all requests originating from a percent of IPs. */
  defaultPolicy?: string | Computed<string>;
  /** The percent of existing sessions to shed from the pool, according to the session policy. */
  sessionPercent?: number | Computed<number>;
  /** Only the hash policy is supported for existing sessions (to avoid exponential decay). */
  sessionPolicy?: string | Computed<string>;
}

export interface LoadBalancingSchemasSingleResponse_NotificationFilter_Origin {
  /** If set true, disable notifications for this type of resource (pool or origin). */
  disable?: boolean | Computed<boolean>;
  /** If present, send notifications only for this health status (e.g. false for only DOWN events). Use null to reset (all events). */
  healthy?: boolean | Computed<boolean>;
}

export interface LoadBalancingSchemasSingleResponse_NotificationFilter {
  /** Filter options for a particular resource type (pool or origin). Use null to reset. */
  origin?: LoadBalancingSchemasSingleResponse_NotificationFilter_Origin | Computed<LoadBalancingSchemasSingleResponse_NotificationFilter_Origin>;
  /** Filter options for a particular resource type (pool or origin). Use null to reset. */
  pool?: LoadBalancingSchemasSingleResponse_NotificationFilter_Origin | Computed<LoadBalancingSchemasSingleResponse_NotificationFilter_Origin>;
}

export interface LoadBalancingSchemasSingleResponse_OriginSteering {
  /** The type of origin steering policy to use. - `"random"`: Select an origin randomly. - `"hash"`: Select an origin by computing a hash over the CF-Connecting-IP address. - `"least_outstanding_requests"`: Select an origin by taking into consideration origin weights, as well as each origin's number of outstanding requests. Origins with more pending requests are weighted proportionately less relative to others. - `"least_connections"`: Select an origin by taking into consideration origin weights, as well as each origin's number of open connections. Origins with more open connections are weighted proportionately less relative to others. Supported for HTTP/1 and HTTP/2 connections. */
  policy?: string | Computed<string>;
}

export interface LoadBalancingSchemasSingleResponse_Origins_Header {
  host?: string[] | Computed<string[]>;
}

export interface LoadBalancingSchemasSingleResponse_Origins {
  address?: string | Computed<string>;
  disabledAt?: string | Computed<string>;
  enabled?: boolean | Computed<boolean>;
  flattenCname?: boolean | Computed<boolean>;
  header?: LoadBalancingSchemasSingleResponse_Origins_Header | Computed<LoadBalancingSchemasSingleResponse_Origins_Header>;
  name?: string | Computed<string>;
  port?: number | Computed<number>;
  virtualNetworkId?: string | Computed<string>;
  weight?: number | Computed<number>;
}

export interface LoadBalancingSchemasSingleResponse_Result {
  /** A list of regions from which to run health checks. Null means every Cloudflare data center. */
  checkRegions?: string[] | Computed<string[]>;
  createdOn?: string | Computed<string>;
  /** A human-readable description of the pool. */
  description?: string | Computed<string>;
  /** This field shows up only if the pool is disabled. This field is set with the time the pool was disabled at. */
  disabledAt?: string | Computed<string>;
  /** Whether to enable (the default) or disable this pool. Disabled pools will not receive traffic and are excluded from health checks. Disabling a pool will cause any load balancers using it to failover to the next pool (if any). */
  enabled?: boolean | Computed<boolean>;
  /** A list of health sources, ordered from highest to lowest priority, used to evaluate individual origin health and overall pool health. The load balancer uses the first source that has data and falls back to the next. Currently accepted values are null or the exact array ["regional", "global"]; any other combination is rejected. Null (the default) behaves like ["local", "global"]. ["regional", "global"] makes each region steer on its own health, falling back to the global decision when a region has no fresh data. Setting regional requires at least one region in check_regions. */
  healthSources?: string[] | Computed<string[]>;
  id?: string | Computed<string>;
  /** The latitude of the data center containing the origins used in this pool in decimal degrees. If this is set, longitude must also be set. */
  latitude?: number | Computed<number>;
  /** Configures load shedding policies and percentages for the pool. */
  loadShedding?: LoadBalancingSchemasSingleResponse_LoadShedding | Computed<LoadBalancingSchemasSingleResponse_LoadShedding>;
  /** The longitude of the data center containing the origins used in this pool in decimal degrees. If this is set, latitude must also be set. */
  longitude?: number | Computed<number>;
  /** The minimum number of origins that must be healthy for this pool to serve traffic. If the number of healthy origins falls below this number, the pool will be marked unhealthy and will failover to the next available pool. */
  minimumOrigins?: number | Computed<number>;
  modifiedOn?: string | Computed<string>;
  /** The ID of the Monitor to use for checking the health of origins within this pool. */
  monitor?: string | Computed<string>;
  /** The ID of the Monitor Group to use for checking the health of origins within this pool. */
  monitorGroup?: string | Computed<string>;
  /** A short name (tag) for the pool. Only alphanumeric characters, hyphens, and underscores are allowed. */
  name?: string | Computed<string>;
  /** List of networks where Load Balancer or Pool is enabled. */
  networks?: string[] | Computed<string[]>;
  /** This field is now deprecated. It has been moved to Cloudflare's Centralized Notification service https://developers.cloudflare.com/fundamentals/notifications/. The email address to send health status notifications to. This can be an individual mailbox or a mailing list. Multiple emails can be supplied as a comma delimited list. */
  notificationEmail?: string | Computed<string>;
  /** Filter pool and origin health notifications by resource type or health status. Use null to reset. */
  notificationFilter?: LoadBalancingSchemasSingleResponse_NotificationFilter | Computed<LoadBalancingSchemasSingleResponse_NotificationFilter>;
  /** Configures origin steering for the pool. Controls how origins are selected for new sessions and traffic without session affinity. */
  originSteering?: LoadBalancingSchemasSingleResponse_OriginSteering | Computed<LoadBalancingSchemasSingleResponse_OriginSteering>;
  /** The list of origins within this pool. Traffic directed at this pool is balanced across all currently healthy origins, provided the pool itself is healthy. */
  origins?: LoadBalancingSchemasSingleResponse_Origins[] | Computed<LoadBalancingSchemasSingleResponse_Origins[]>;
}

const LoadBalancingSchemasSingleResponse_LoadSheddingFields: FieldMap = {
  defaultPercent: "default_percent",
  defaultPolicy: "default_policy",
  sessionPercent: "session_percent",
  sessionPolicy: "session_policy",
};

const LoadBalancingSchemasSingleResponse_NotificationFilter_OriginFields: FieldMap = {
  disable: "disable",
  healthy: "healthy",
};

const LoadBalancingSchemasSingleResponse_NotificationFilterFields: FieldMap = {
  origin: {
    wireName: "origin",
    kind: "object",
    fields: LoadBalancingSchemasSingleResponse_NotificationFilter_OriginFields,
  },
  pool: {
    wireName: "pool",
    kind: "object",
    fields: LoadBalancingSchemasSingleResponse_NotificationFilter_OriginFields,
  },
};

const LoadBalancingSchemasSingleResponse_OriginSteeringFields: FieldMap = {
  policy: "policy",
};

const LoadBalancingSchemasSingleResponse_Origins_HeaderFields: FieldMap = {
  host: "host",
};

const LoadBalancingSchemasSingleResponse_OriginsFields: FieldMap = {
  address: "address",
  disabledAt: "disabled_at",
  enabled: "enabled",
  flattenCname: "flatten_cname",
  header: {
    wireName: "header",
    kind: "object",
    fields: LoadBalancingSchemasSingleResponse_Origins_HeaderFields,
  },
  name: "name",
  port: "port",
  virtualNetworkId: "virtual_network_id",
  weight: "weight",
};

export interface LoadBalancingSchemasSingleResponseConfig {
  /** A list of regions from which to run health checks. Null means every Cloudflare data center. */
  checkRegions?: string[] | Computed<string[]>;
  /** A human-readable description of the pool. */
  description?: string | Computed<string>;
  /** Whether to enable (the default) or disable this pool. Disabled pools will not receive traffic and are excluded from health checks. Disabling a pool will cause any load balancers using it to failover to the next pool (if any). */
  enabled?: boolean | Computed<boolean>;
  /** A list of health sources, ordered from highest to lowest priority, used to evaluate individual origin health and overall pool health. The load balancer uses the first source that has data and falls back to the next. Currently accepted values are null or the exact array ["regional", "global"]; any other combination is rejected. Null (the default) behaves like ["local", "global"]. ["regional", "global"] makes each region steer on its own health, falling back to the global decision when a region has no fresh data. Setting regional requires at least one region in check_regions. */
  healthSources?: string[] | Computed<string[]>;
  /** The latitude of the data center containing the origins used in this pool in decimal degrees. If this is set, longitude must also be set. */
  latitude?: number | Computed<number>;
  /** Configures load shedding policies and percentages for the pool. */
  loadShedding?: LoadBalancingSchemasSingleResponse_LoadShedding | Computed<LoadBalancingSchemasSingleResponse_LoadShedding>;
  /** The longitude of the data center containing the origins used in this pool in decimal degrees. If this is set, latitude must also be set. */
  longitude?: number | Computed<number>;
  /** The minimum number of origins that must be healthy for this pool to serve traffic. If the number of healthy origins falls below this number, the pool will be marked unhealthy and will failover to the next available pool. */
  minimumOrigins?: number | Computed<number>;
  /** The ID of the Monitor to use for checking the health of origins within this pool. */
  monitor?: string | Computed<string>;
  /** The ID of the Monitor Group to use for checking the health of origins within this pool. */
  monitorGroup?: string | Computed<string>;
  /** A short name (tag) for the pool. Only alphanumeric characters, hyphens, and underscores are allowed. */
  name: string | Computed<string>;
  /** List of networks where Load Balancer or Pool is enabled. */
  networks?: string[] | Computed<string[]>;
  /** This field is now deprecated. It has been moved to Cloudflare's Centralized Notification service https://developers.cloudflare.com/fundamentals/notifications/. The email address to send health status notifications to. This can be an individual mailbox or a mailing list. Multiple emails can be supplied as a comma delimited list. */
  notificationEmail?: string | Computed<string>;
  /** Filter pool and origin health notifications by resource type or health status. Use null to reset. */
  notificationFilter?: LoadBalancingSchemasSingleResponse_NotificationFilter | Computed<LoadBalancingSchemasSingleResponse_NotificationFilter>;
  /** Configures origin steering for the pool. Controls how origins are selected for new sessions and traffic without session affinity. */
  originSteering?: LoadBalancingSchemasSingleResponse_OriginSteering | Computed<LoadBalancingSchemasSingleResponse_OriginSteering>;
  /** The list of origins within this pool. Traffic directed at this pool is balanced across all currently healthy origins, provided the pool itself is healthy. */
  origins: LoadBalancingSchemasSingleResponse_Origins[] | Computed<LoadBalancingSchemasSingleResponse_Origins[]>;
  /** path parameter, not part of the API's own resource representation */
  accountId: string | Computed<string>;
  /** path parameter, not part of the API's own resource representation */
  poolId: string | Computed<string>;
}

export interface LoadBalancingSchemasSingleResponseAttrs {
  /** A list of regions from which to run health checks. Null means every Cloudflare data center. */
  checkRegions: string[];
  /** A human-readable description of the pool. */
  description: string;
  /** Whether to enable (the default) or disable this pool. Disabled pools will not receive traffic and are excluded from health checks. Disabling a pool will cause any load balancers using it to failover to the next pool (if any). */
  enabled: boolean;
  /** A list of health sources, ordered from highest to lowest priority, used to evaluate individual origin health and overall pool health. The load balancer uses the first source that has data and falls back to the next. Currently accepted values are null or the exact array ["regional", "global"]; any other combination is rejected. Null (the default) behaves like ["local", "global"]. ["regional", "global"] makes each region steer on its own health, falling back to the global decision when a region has no fresh data. Setting regional requires at least one region in check_regions. */
  healthSources: string[];
  /** The latitude of the data center containing the origins used in this pool in decimal degrees. If this is set, longitude must also be set. */
  latitude: number;
  /** Configures load shedding policies and percentages for the pool. */
  loadShedding: LoadBalancingSchemasSingleResponse_LoadShedding;
  /** The longitude of the data center containing the origins used in this pool in decimal degrees. If this is set, latitude must also be set. */
  longitude: number;
  /** The minimum number of origins that must be healthy for this pool to serve traffic. If the number of healthy origins falls below this number, the pool will be marked unhealthy and will failover to the next available pool. */
  minimumOrigins: number;
  /** The ID of the Monitor to use for checking the health of origins within this pool. */
  monitor: string;
  /** The ID of the Monitor Group to use for checking the health of origins within this pool. */
  monitorGroup: string;
  /** A short name (tag) for the pool. Only alphanumeric characters, hyphens, and underscores are allowed. */
  name: string;
  /** List of networks where Load Balancer or Pool is enabled. */
  networks: string[];
  /** This field is now deprecated. It has been moved to Cloudflare's Centralized Notification service https://developers.cloudflare.com/fundamentals/notifications/. The email address to send health status notifications to. This can be an individual mailbox or a mailing list. Multiple emails can be supplied as a comma delimited list. */
  notificationEmail: string;
  /** Filter pool and origin health notifications by resource type or health status. Use null to reset. */
  notificationFilter: LoadBalancingSchemasSingleResponse_NotificationFilter;
  /** Configures origin steering for the pool. Controls how origins are selected for new sessions and traffic without session affinity. */
  originSteering: LoadBalancingSchemasSingleResponse_OriginSteering;
  /** The list of origins within this pool. Traffic directed at this pool is balanced across all currently healthy origins, provided the pool itself is healthy. */
  origins: LoadBalancingSchemasSingleResponse_Origins[];
  /** The requested resource, present when the API call succeeds. (AI-inferred) */
  result: LoadBalancingSchemasSingleResponse_Result;
  /** path parameter, not part of the API's own resource representation */
  accountId: string;
  /** path parameter, not part of the API's own resource representation */
  poolId: string;
}

export const LoadBalancingSchemasSingleResponse: ResourceBinding<LoadBalancingSchemasSingleResponseConfig, LoadBalancingSchemasSingleResponseAttrs> = {
  wireType: "cloudflare_load_balancing_schemas_single_response",
  fields: {
    checkRegions: "check_regions",
    description: "description",
    enabled: "enabled",
    healthSources: "health_sources",
    latitude: "latitude",
    loadShedding: {
      wireName: "load_shedding",
      kind: "object",
      fields: LoadBalancingSchemasSingleResponse_LoadSheddingFields,
    },
    longitude: "longitude",
    minimumOrigins: "minimum_origins",
    monitor: "monitor",
    monitorGroup: "monitor_group",
    name: "name",
    networks: "networks",
    notificationEmail: "notification_email",
    notificationFilter: {
      wireName: "notification_filter",
      kind: "object",
      fields: LoadBalancingSchemasSingleResponse_NotificationFilterFields,
    },
    originSteering: {
      wireName: "origin_steering",
      kind: "object",
      fields: LoadBalancingSchemasSingleResponse_OriginSteeringFields,
    },
    origins: {
      wireName: "origins",
      kind: "list",
      fields: LoadBalancingSchemasSingleResponse_OriginsFields,
    },
    accountId: "account_id",
    poolId: "pool_id",
  },
};

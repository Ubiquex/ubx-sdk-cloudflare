// Code generated by `ubx sdk gen` from cloudflare_recording. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface Recording_AudioConfig {
  /** Audio signal pathway within an audio file that carries a specific sound source. */
  channel?: string | Computed<string>;
  /** Codec using which the recording will be encoded. If VP8/VP9 is selected for videoConfig, changing audioConfig is not allowed. In this case, the codec in the audioConfig is automatically set to vorbis. */
  codec?: string | Computed<string>;
  /** Controls whether to export audio file seperately */
  exportFile?: boolean | Computed<boolean>;
}

export interface Recording_InteractiveConfig {
  /** The metadata is presented in the form of ID3 tags. */
  type?: string | Computed<string>;
}

export interface Recording_RealtimekitBucketConfig {
  /** Controls whether recordings are uploaded to RealtimeKit's bucket. If set to false, `download_url`, `audio_download_url`, `download_url_expiry` won't be generated for a recording. */
  enabled: boolean | Computed<boolean>;
}

export interface Recording_RtmpOutConfig {
  /** RTMP URL to stream to */
  rtmpUrl?: string | Computed<string>;
}

export interface Recording_StorageConfig {
  /** Access key of the storage medium. Access key is not required for the `gcs` storage media type. Note that this field is not readable by clients, only writeable. */
  accessKey?: string | Computed<string>;
  /** Authentication method used for "sftp" type storage medium */
  authMethod?: string | Computed<string>;
  /** Name of the storage medium's bucket. */
  bucket: string | Computed<string>;
  /** SSH destination server host for SFTP type storage medium */
  host?: string | Computed<string>;
  /** SSH destination server password for SFTP type storage medium when auth_method is "PASSWORD". If auth_method is "KEY", this specifies the password for the ssh private key. */
  password?: string | Computed<string>;
  /** Path relative to the bucket root at which the recording will be placed. */
  path?: string | Computed<string>;
  /** SSH destination server port for SFTP type storage medium */
  port?: number | Computed<number>;
  /** Private key used to login to destination SSH server for SFTP type storage medium, when auth_method used is "KEY" */
  privateKey?: string | Computed<string>;
  /** Region of the storage medium. */
  region?: string | Computed<string>;
  /** Secret key of the storage medium. Similar to `access_key`, it is only writeable by clients, not readable. */
  secret?: string | Computed<string>;
  /** Type of storage media. */
  type: string | Computed<string>;
  /** SSH destination server username for SFTP type storage medium */
  username?: string | Computed<string>;
}

export interface Recording_VideoConfig_Watermark_Size {
  /** Height of the watermark in px */
  height?: number | Computed<number>;
  /** Width of the watermark in px */
  width?: number | Computed<number>;
}

export interface Recording_VideoConfig_Watermark {
  /** Position of the watermark */
  position?: string | Computed<string>;
  /** Size of the watermark */
  size?: Recording_VideoConfig_Watermark_Size | Computed<Recording_VideoConfig_Watermark_Size>;
  /** URL of the watermark image */
  url?: string | Computed<string>;
}

export interface Recording_VideoConfig {
  /** Codec using which the recording will be encoded. */
  codec?: string | Computed<string>;
  /** Controls whether to export video file seperately */
  exportFile?: boolean | Computed<boolean>;
  /** Height of the recording video in pixels */
  height?: number | Computed<number>;
  /** Watermark to be added to the recording */
  watermark?: Recording_VideoConfig_Watermark | Computed<Recording_VideoConfig_Watermark>;
  /** Width of the recording video in pixels */
  width?: number | Computed<number>;
}

const Recording_AudioConfigFields: FieldMap = {
  channel: "channel",
  codec: "codec",
  exportFile: "export_file",
};

const Recording_InteractiveConfigFields: FieldMap = {
  type: "type",
};

const Recording_RealtimekitBucketConfigFields: FieldMap = {
  enabled: "enabled",
};

const Recording_RtmpOutConfigFields: FieldMap = {
  rtmpUrl: "rtmp_url",
};

const Recording_StorageConfigFields: FieldMap = {
  accessKey: "access_key",
  authMethod: "auth_method",
  bucket: "bucket",
  host: "host",
  password: "password",
  path: "path",
  port: "port",
  privateKey: "private_key",
  region: "region",
  secret: "secret",
  type: "type",
  username: "username",
};

const Recording_VideoConfig_Watermark_SizeFields: FieldMap = {
  height: "height",
  width: "width",
};

const Recording_VideoConfig_WatermarkFields: FieldMap = {
  position: "position",
  size: {
    wireName: "size",
    kind: "object",
    fields: Recording_VideoConfig_Watermark_SizeFields,
  },
  url: "url",
};

const Recording_VideoConfigFields: FieldMap = {
  codec: "codec",
  exportFile: "export_file",
  height: "height",
  watermark: {
    wireName: "watermark",
    kind: "object",
    fields: Recording_VideoConfig_WatermarkFields,
  },
  width: "width",
};

export interface RecordingConfig {
  /** By default, a meeting allows only one recording to run at a time. Enabling the `allow_multiple_recordings` parameter to true allows you to initiate multiple recordings concurrently in the same meeting. This allows you to record separate videos of the same meeting with different configurations, such as portrait mode or landscape mode. */
  allowMultipleRecordings?: boolean | Computed<boolean>;
  /** Object containing configuration regarding the audio that is being recorded. */
  audioConfig?: Recording_AudioConfig | Computed<Recording_AudioConfig>;
  /** Update the recording file name. */
  fileNamePrefix?: string | Computed<string>;
  /** Allows you to add timed metadata to your recordings, which are digital markers inserted into a video file to provide contextual information at specific points in the content range. The ID3 tags containing this information are available to clients on the playback timeline in HLS format. The output files are generated in a compressed .tar format. */
  interactiveConfig?: Recording_InteractiveConfig | Computed<Recording_InteractiveConfig>;
  /** Specifies the maximum duration for recording in seconds, ranging from a minimum of 60 seconds to a maximum of 24 hours. */
  maxSeconds?: number | Computed<number>;
  /** ID of the meeting to record. */
  meetingId: string | Computed<string>;
  realtimekitBucketConfig?: Recording_RealtimekitBucketConfig | Computed<Recording_RealtimekitBucketConfig>;
  rtmpOutConfig?: Recording_RtmpOutConfig | Computed<Recording_RtmpOutConfig>;
  storageConfig?: Recording_StorageConfig | Computed<Recording_StorageConfig>;
  /** Pass a custom url to record arbitary screen */
  url?: string | Computed<string>;
  videoConfig?: Recording_VideoConfig | Computed<Recording_VideoConfig>;
  /** path parameter, not part of the API's own resource representation */
  accountId: string | Computed<string>;
  /** path parameter, not part of the API's own resource representation */
  appId: string | Computed<string>;
  /** path parameter, not part of the API's own resource representation */
  recordingId: string | Computed<string>;
}

export interface RecordingAttrs {
  /** By default, a meeting allows only one recording to run at a time. Enabling the `allow_multiple_recordings` parameter to true allows you to initiate multiple recordings concurrently in the same meeting. This allows you to record separate videos of the same meeting with different configurations, such as portrait mode or landscape mode. */
  allowMultipleRecordings: boolean;
  /** Object containing configuration regarding the audio that is being recorded. */
  audioConfig: Recording_AudioConfig;
  /** Update the recording file name. */
  fileNamePrefix: string;
  /** Allows you to add timed metadata to your recordings, which are digital markers inserted into a video file to provide contextual information at specific points in the content range. The ID3 tags containing this information are available to clients on the playback timeline in HLS format. The output files are generated in a compressed .tar format. */
  interactiveConfig: Recording_InteractiveConfig;
  /** Specifies the maximum duration for recording in seconds, ranging from a minimum of 60 seconds to a maximum of 24 hours. */
  maxSeconds: number;
  /** ID of the meeting to record. */
  meetingId: string;
  realtimekitBucketConfig: Recording_RealtimekitBucketConfig;
  rtmpOutConfig: Recording_RtmpOutConfig;
  storageConfig: Recording_StorageConfig;
  /** Pass a custom url to record arbitary screen */
  url: string;
  videoConfig: Recording_VideoConfig;
  /** path parameter, not part of the API's own resource representation */
  accountId: string;
  /** path parameter, not part of the API's own resource representation */
  appId: string;
  /** path parameter, not part of the API's own resource representation */
  recordingId: string;
}

export const Recording: ResourceBinding<RecordingConfig, RecordingAttrs> = {
  wireType: "cloudflare_recording",
  fields: {
    allowMultipleRecordings: "allow_multiple_recordings",
    audioConfig: {
      wireName: "audio_config",
      kind: "object",
      fields: Recording_AudioConfigFields,
    },
    fileNamePrefix: "file_name_prefix",
    interactiveConfig: {
      wireName: "interactive_config",
      kind: "object",
      fields: Recording_InteractiveConfigFields,
    },
    maxSeconds: "max_seconds",
    meetingId: "meeting_id",
    realtimekitBucketConfig: {
      wireName: "realtimekit_bucket_config",
      kind: "object",
      fields: Recording_RealtimekitBucketConfigFields,
    },
    rtmpOutConfig: {
      wireName: "rtmp_out_config",
      kind: "object",
      fields: Recording_RtmpOutConfigFields,
    },
    storageConfig: {
      wireName: "storage_config",
      kind: "object",
      fields: Recording_StorageConfigFields,
    },
    url: "url",
    videoConfig: {
      wireName: "video_config",
      kind: "object",
      fields: Recording_VideoConfigFields,
    },
    accountId: "account_id",
    appId: "app_id",
    recordingId: "recording_id",
  },
};

// Code generated by `ubx sdk gen` from cloudflare_tls_certificates_and_hostnames_certificate_response_single. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface TlsCertificatesAndHostnamesCertificateResponseSingle_GeoRestrictions {
  label?: string | Computed<string>;
}

const TlsCertificatesAndHostnamesCertificateResponseSingle_GeoRestrictionsFields: FieldMap = {
  label: "label",
};

export interface TlsCertificatesAndHostnamesCertificateResponseSingleConfig {
  /** A ubiquitous bundle has the highest probability of being verified everywhere, even by clients using outdated or unusual trust stores. An optimal bundle uses the shortest chain and newest intermediates. And the force bundle verifies the chain, but does not otherwise modify it. */
  bundleMethod?: string | Computed<string>;
  /** The zone's SSL certificate or certificate and the intermediate(s). */
  certificate: string | Computed<string>;
  /** The identifier for the Custom CSR that was used. */
  customCsrId?: string | Computed<string>;
  /** The environment to deploy the certificate to, defaults to production. */
  deploy?: string | Computed<string>;
  /** Specify the region where your private key can be held locally for optimal TLS performance. HTTPS connections to any excluded data center will still be fully encrypted, but will incur some latency while Keyless SSL is used to complete the handshake with the nearest allowed data center. Options allow distribution to only to U.S. data centers, only to E.U. data centers, or only to highest security data centers. Default distribution is to all Cloudflare datacenters, for optimal performance. */
  geoRestrictions?: TlsCertificatesAndHostnamesCertificateResponseSingle_GeoRestrictions | Computed<TlsCertificatesAndHostnamesCertificateResponseSingle_GeoRestrictions>;
  /** Specify the policy that determines the region where your private key will be held locally. HTTPS connections to any excluded data center will still be fully encrypted, but will incur some latency while Keyless SSL is used to complete the handshake with the nearest allowed data center. Any combination of countries, specified by their two letter country code (https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2#Officially_assigned_code_elements) can be chosen, such as 'country: IN', as well as 'region: EU' which refers to the EU region. If there are too few data centers satisfying the policy, it will be rejected. Note: The API accepts this field as either "policy" or "policy_restrictions" in requests. Responses return this field as "policy_restrictions". */
  policy?: string | Computed<string>;
  /** The zone's private key. Not required if custom_csr_id is provided, in which case the private key is retrieved from the CSR record held by Cloudflare. */
  privateKey?: string | Computed<string>;
  /** The type 'legacy_custom' enables support for legacy clients which do not include SNI in the TLS handshake. */
  type?: string | Computed<string>;
  /** path parameter, not part of the API's own resource representation */
  zoneId: string | Computed<string>;
  /** path parameter, not part of the API's own resource representation */
  customCertificateId: string | Computed<string>;
}

export interface TlsCertificatesAndHostnamesCertificateResponseSingleAttrs {
  /** A ubiquitous bundle has the highest probability of being verified everywhere, even by clients using outdated or unusual trust stores. An optimal bundle uses the shortest chain and newest intermediates. And the force bundle verifies the chain, but does not otherwise modify it. */
  bundleMethod: string;
  /** The zone's SSL certificate or certificate and the intermediate(s). */
  certificate: string;
  /** The identifier for the Custom CSR that was used. */
  customCsrId: string;
  /** The environment to deploy the certificate to, defaults to production. */
  deploy: string;
  /** Specify the region where your private key can be held locally for optimal TLS performance. HTTPS connections to any excluded data center will still be fully encrypted, but will incur some latency while Keyless SSL is used to complete the handshake with the nearest allowed data center. Options allow distribution to only to U.S. data centers, only to E.U. data centers, or only to highest security data centers. Default distribution is to all Cloudflare datacenters, for optimal performance. */
  geoRestrictions: TlsCertificatesAndHostnamesCertificateResponseSingle_GeoRestrictions;
  /** Specify the policy that determines the region where your private key will be held locally. HTTPS connections to any excluded data center will still be fully encrypted, but will incur some latency while Keyless SSL is used to complete the handshake with the nearest allowed data center. Any combination of countries, specified by their two letter country code (https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2#Officially_assigned_code_elements) can be chosen, such as 'country: IN', as well as 'region: EU' which refers to the EU region. If there are too few data centers satisfying the policy, it will be rejected. Note: The API accepts this field as either "policy" or "policy_restrictions" in requests. Responses return this field as "policy_restrictions". */
  policy: string;
  /** The zone's private key. Not required if custom_csr_id is provided, in which case the private key is retrieved from the CSR record held by Cloudflare. */
  privateKey: string;
  /** The type 'legacy_custom' enables support for legacy clients which do not include SNI in the TLS handshake. */
  type: string;
  /** path parameter, not part of the API's own resource representation */
  zoneId: string;
  /** path parameter, not part of the API's own resource representation */
  customCertificateId: string;
}

export const TlsCertificatesAndHostnamesCertificateResponseSingle: ResourceBinding<TlsCertificatesAndHostnamesCertificateResponseSingleConfig, TlsCertificatesAndHostnamesCertificateResponseSingleAttrs> = {
  wireType: "cloudflare_tls_certificates_and_hostnames_certificate_response_single",
  fields: {
    bundleMethod: "bundle_method",
    certificate: "certificate",
    customCsrId: "custom_csr_id",
    deploy: "deploy",
    geoRestrictions: {
      wireName: "geo_restrictions",
      kind: "object",
      fields: TlsCertificatesAndHostnamesCertificateResponseSingle_GeoRestrictionsFields,
    },
    policy: "policy",
    privateKey: "private_key",
    type: "type",
    zoneId: "zone_id",
    customCertificateId: "custom_certificate_id",
  },
};

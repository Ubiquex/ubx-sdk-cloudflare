// Code generated by `ubx sdk gen` from cloudflare_tls_certificates_and_hostnames_certificate_response_single_2. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface TlsCertificatesAndHostnamesCertificateResponseSingle2Config {
  /** The Certificate Signing Request (CSR). Must be newline-encoded. */
  csr: string | Computed<string>;
  /** Array of hostnames or wildcard names bound to the certificate. Hostnames must be fully qualified domain names (FQDNs) belonging to zones on your account (e.g., `example.com` or `sub.example.com`). Wildcards are supported only as a `*.` prefix for a single level (e.g., `*.example.com`). Double wildcards (`*.*.example.com`) and interior wildcards (`foo.*.example.com`) are not allowed. The wildcard suffix must be a multi-label domain (`*.example.com` is valid, but `*.com` is not). Unicode/IDN hostnames are accepted and automatically converted to punycode. */
  hostnames: string[] | Computed<string[]>;
  /** Signature type desired on certificate ("origin-rsa" (rsa), "origin-ecc" (ecdsa), or "keyless-certificate" (for Keyless SSL servers). */
  requestType: string | Computed<string>;
  /** The number of days for which the certificate should be valid. */
  requestedValidity?: number | Computed<number>;
  /** path parameter, not part of the API's own resource representation */
  certificateId: string | Computed<string>;
}

export interface TlsCertificatesAndHostnamesCertificateResponseSingle2Attrs {
  /** The Certificate Signing Request (CSR). Must be newline-encoded. */
  csr: string;
  /** Array of hostnames or wildcard names bound to the certificate. Hostnames must be fully qualified domain names (FQDNs) belonging to zones on your account (e.g., `example.com` or `sub.example.com`). Wildcards are supported only as a `*.` prefix for a single level (e.g., `*.example.com`). Double wildcards (`*.*.example.com`) and interior wildcards (`foo.*.example.com`) are not allowed. The wildcard suffix must be a multi-label domain (`*.example.com` is valid, but `*.com` is not). Unicode/IDN hostnames are accepted and automatically converted to punycode. */
  hostnames: string[];
  /** Signature type desired on certificate ("origin-rsa" (rsa), "origin-ecc" (ecdsa), or "keyless-certificate" (for Keyless SSL servers). */
  requestType: string;
  /** The number of days for which the certificate should be valid. */
  requestedValidity: number;
  /** path parameter, not part of the API's own resource representation */
  certificateId: string;
}

export const TlsCertificatesAndHostnamesCertificateResponseSingle2: ResourceBinding<TlsCertificatesAndHostnamesCertificateResponseSingle2Config, TlsCertificatesAndHostnamesCertificateResponseSingle2Attrs> = {
  wireType: "cloudflare_tls_certificates_and_hostnames_certificate_response_single_2",
  fields: {
    csr: "csr",
    hostnames: "hostnames",
    requestType: "request_type",
    requestedValidity: "requested_validity",
    certificateId: "certificate_id",
  },
};

// Code generated by `ubx sdk gen` from cloudflare_firewall_override_response_single. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface FirewallOverrideResponseSingle_Result_RewriteAction {
  /** The WAF rule action to apply. */
  block?: unknown | Computed<unknown>;
  /** The WAF rule action to apply. */
  challenge?: unknown | Computed<unknown>;
  /** The WAF rule action to apply. */
  default?: unknown | Computed<unknown>;
  /** The WAF rule action to apply. */
  disable?: unknown | Computed<unknown>;
  /** The WAF rule action to apply. */
  simulate?: unknown | Computed<unknown>;
}

export interface FirewallOverrideResponseSingle_Result_Rules {
}

export interface FirewallOverrideResponseSingle_Result {
  /** An informative summary of the current URI-based WAF override. */
  description?: string | Computed<string>;
  /** An object that allows you to enable or disable WAF rule groups for the current WAF override. Each key of this object must be the ID of a WAF rule group, and each value must be a valid WAF action (usually `default` or `disable`). When creating a new URI-based WAF override, you must provide a `groups` object or a `rules` object. */
  groups?: unknown | Computed<unknown>;
  /** The unique identifier of the WAF override. */
  id?: string | Computed<string>;
  /** When true, indicates that the rule is currently paused. */
  paused?: boolean | Computed<boolean>;
  /** The relative priority of the current URI-based WAF override when multiple overrides match a single URL. A lower number indicates higher priority. Higher priority overrides may overwrite values set by lower priority overrides. */
  priority?: number | Computed<number>;
  /** Specifies that, when a WAF rule matches, its configured action will be replaced by the action configured in this object. */
  rewriteAction?: FirewallOverrideResponseSingle_Result_RewriteAction | Computed<FirewallOverrideResponseSingle_Result_RewriteAction>;
  /** An object that allows you to override the action of specific WAF rules. Each key of this object must be the ID of a WAF rule, and each value must be a valid WAF action. Unless you are disabling a rule, ensure that you also enable the rule group that this WAF rule belongs to. When creating a new URI-based WAF override, you must provide a `groups` object or a `rules` object. */
  rules?: Record<string, FirewallOverrideResponseSingle_Result_Rules> | Computed<Record<string, FirewallOverrideResponseSingle_Result_Rules>>;
  /** The URLs to include in the current WAF override. You can use wildcards. Each entered URL will be escaped before use, which means you can only use simple wildcard patterns. */
  urls?: string[] | Computed<string[]>;
}

export interface FirewallOverrideResponseSingleConfig {
  /** The URLs to include in the current WAF override. You can use wildcards. Each entered URL will be escaped before use, which means you can only use simple wildcard patterns. */
  urls: string[] | Computed<string[]>;
  /** path parameter, not part of the API's own resource representation */
  zoneId: string | Computed<string>;
  /** path parameter, not part of the API's own resource representation */
  overridesId: string | Computed<string>;
}

export interface FirewallOverrideResponseSingleAttrs {
  result: FirewallOverrideResponseSingle_Result;
  /** The URLs to include in the current WAF override. You can use wildcards. Each entered URL will be escaped before use, which means you can only use simple wildcard patterns. */
  urls: string[];
  /** path parameter, not part of the API's own resource representation */
  zoneId: string;
  /** path parameter, not part of the API's own resource representation */
  overridesId: string;
}

export const FirewallOverrideResponseSingle: ResourceBinding<FirewallOverrideResponseSingleConfig, FirewallOverrideResponseSingleAttrs> = {
  wireType: "cloudflare_firewall_override_response_single",
  fields: {
    urls: "urls",
    zoneId: "zone_id",
    overridesId: "overrides_id",
  },
};

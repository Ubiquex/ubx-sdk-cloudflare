// Code generated by `ubx sdk gen` from cloudflare_smartshield_single_hc_response. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface SmartshieldSingleHcResponse_HttpConfig {
  /** Do not validate the certificate when the health check uses HTTPS. */
  allowInsecure?: boolean | Computed<boolean>;
  /** A case-insensitive sub-string to look for in the response body. If this string is not found, the origin will be marked as unhealthy. */
  expectedBody?: string | Computed<string>;
  /** The expected HTTP response codes (e.g. "200") or code ranges (e.g. "2xx" for all codes starting with 2) of the health check. */
  expectedCodes?: string[] | Computed<string[]>;
  /** Follow redirects if the origin returns a 3xx status code. */
  followRedirects?: boolean | Computed<boolean>;
  /** The HTTP request headers to send in the health check. It is recommended you set a Host header by default. The User-Agent header cannot be overridden. */
  header?: Record<string, string[]> | Computed<Record<string, string[]>>;
  /** The HTTP method to use for the health check. */
  method?: string | Computed<string>;
  /** The endpoint path to health check against. */
  path?: string | Computed<string>;
  /** Port number to connect to for the health check. Defaults to 80 if type is HTTP or 443 if type is HTTPS. */
  port?: number | Computed<number>;
}

export interface SmartshieldSingleHcResponse_TcpConfig {
  /** The TCP connection method to use for the health check. */
  method?: string | Computed<string>;
  /** Port number to connect to for the health check. Defaults to 80. */
  port?: number | Computed<number>;
}

const SmartshieldSingleHcResponse_HttpConfigFields: FieldMap = {
  allowInsecure: "allow_insecure",
  expectedBody: "expected_body",
  expectedCodes: "expected_codes",
  followRedirects: "follow_redirects",
  header: "header",
  method: "method",
  path: "path",
  port: "port",
};

const SmartshieldSingleHcResponse_TcpConfigFields: FieldMap = {
  method: "method",
  port: "port",
};

export interface SmartshieldSingleHcResponseConfig {
  /** The hostname or IP address of the origin server to run health checks on. */
  address: string | Computed<string>;
  /** A list of regions from which to run health checks. Null means Cloudflare will pick a default region. */
  checkRegions?: string[] | Computed<string[]>;
  /** The number of consecutive fails required from a health check before changing the health to unhealthy. */
  consecutiveFails?: number | Computed<number>;
  /** The number of consecutive successes required from a health check before changing the health to healthy. */
  consecutiveSuccesses?: number | Computed<number>;
  /** A human-readable description of the health check. */
  description?: string | Computed<string>;
  /** Parameters specific to an HTTP or HTTPS health check. */
  httpConfig?: SmartshieldSingleHcResponse_HttpConfig | Computed<SmartshieldSingleHcResponse_HttpConfig>;
  /** The interval between each health check. Shorter intervals may give quicker notifications if the origin status changes, but will increase load on the origin as we check from multiple locations. */
  interval?: number | Computed<number>;
  /** A short name to identify the health check. Only alphanumeric characters, hyphens and underscores are allowed. */
  name: string | Computed<string>;
  /** The number of retries to attempt in case of a timeout before marking the origin as unhealthy. Retries are attempted immediately. */
  retries?: number | Computed<number>;
  /** If suspended, no health checks are sent to the origin. */
  suspended?: boolean | Computed<boolean>;
  /** Parameters specific to TCP health check. */
  tcpConfig?: SmartshieldSingleHcResponse_TcpConfig | Computed<SmartshieldSingleHcResponse_TcpConfig>;
  /** The timeout (in seconds) before marking the health check as failed. */
  timeout?: number | Computed<number>;
  /** The protocol to use for the health check. Currently supported protocols are 'HTTP', 'HTTPS' and 'TCP'. */
  type?: string | Computed<string>;
  /** path parameter, not part of the API's own resource representation */
  zoneId: string | Computed<string>;
  /** path parameter, not part of the API's own resource representation */
  healthcheckId: string | Computed<string>;
}

export interface SmartshieldSingleHcResponseAttrs {
  /** The hostname or IP address of the origin server to run health checks on. */
  address: string;
  /** A list of regions from which to run health checks. Null means Cloudflare will pick a default region. */
  checkRegions: string[];
  /** The number of consecutive fails required from a health check before changing the health to unhealthy. */
  consecutiveFails: number;
  /** The number of consecutive successes required from a health check before changing the health to healthy. */
  consecutiveSuccesses: number;
  /** A human-readable description of the health check. */
  description: string;
  /** Parameters specific to an HTTP or HTTPS health check. */
  httpConfig: SmartshieldSingleHcResponse_HttpConfig;
  /** The interval between each health check. Shorter intervals may give quicker notifications if the origin status changes, but will increase load on the origin as we check from multiple locations. */
  interval: number;
  /** A short name to identify the health check. Only alphanumeric characters, hyphens and underscores are allowed. */
  name: string;
  /** The number of retries to attempt in case of a timeout before marking the origin as unhealthy. Retries are attempted immediately. */
  retries: number;
  /** If suspended, no health checks are sent to the origin. */
  suspended: boolean;
  /** Parameters specific to TCP health check. */
  tcpConfig: SmartshieldSingleHcResponse_TcpConfig;
  /** The timeout (in seconds) before marking the health check as failed. */
  timeout: number;
  /** The protocol to use for the health check. Currently supported protocols are 'HTTP', 'HTTPS' and 'TCP'. */
  type: string;
  /** path parameter, not part of the API's own resource representation */
  zoneId: string;
  /** path parameter, not part of the API's own resource representation */
  healthcheckId: string;
}

export const SmartshieldSingleHcResponse: ResourceBinding<SmartshieldSingleHcResponseConfig, SmartshieldSingleHcResponseAttrs> = {
  wireType: "cloudflare_smartshield_single_hc_response",
  fields: {
    address: "address",
    checkRegions: "check_regions",
    consecutiveFails: "consecutive_fails",
    consecutiveSuccesses: "consecutive_successes",
    description: "description",
    httpConfig: {
      wireName: "http_config",
      kind: "object",
      fields: SmartshieldSingleHcResponse_HttpConfigFields,
    },
    interval: "interval",
    name: "name",
    retries: "retries",
    suspended: "suspended",
    tcpConfig: {
      wireName: "tcp_config",
      kind: "object",
      fields: SmartshieldSingleHcResponse_TcpConfigFields,
    },
    timeout: "timeout",
    type: "type",
    zoneId: "zone_id",
    healthcheckId: "healthcheck_id",
  },
};

// Code generated by `ubx sdk gen` from cloudflare_bot. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface Bot_Result_Bot {
  /** The category of the bot. */
  category: string | Computed<string>;
  /** A summary for the bot (e.g., purpose). */
  description: string | Computed<string>;
  /** The kind of the bot. */
  kind: string | Computed<string>;
  /** The name of the bot. */
  name: string | Computed<string>;
  /** The organization that owns and operates the bot. */
  operator: string | Computed<string>;
  /** The link to the bot documentation. */
  operatorUrl: string | Computed<string>;
  /** The URL of the agent's [Web Bot Auth](https://blog.cloudflare.com/web-bot-auth/) resource. Null for bots not verified via request signature. */
  signatureAgentUrl?: string | Computed<string>;
  /** A kebab-case identifier derived from the bot name. */
  slug: string | Computed<string>;
  userAgentPatterns: string[] | Computed<string[]>;
  userAgents: string[] | Computed<string[]>;
}

export interface Bot_Result {
  bot: Bot_Result_Bot | Computed<Bot_Result_Bot>;
}

export interface BotConfig {
  /** Can set the creator field with an internal user ID. */
  creator?: string | Computed<string>;
  /** An image binary data. Only needed when type is uploading a file. */
  file?: string | Computed<string>;
  /** Optional Image Custom ID. Up to 1024 chars. Can include any number of subpaths, and utf8 characters. Cannot start nor end with a / (forward slash). Cannot be a UUID. */
  id?: string | Computed<string>;
  /** User modifiable key-value store. Can use used for keeping references to another system of record for managing images. */
  metadata?: unknown | Computed<unknown>;
  /** Indicates whether the image requires a signature token for the access. */
  requireSignedUrls?: boolean | Computed<boolean>;
  /** A URL to fetch an image from origin. Only needed when type is uploading from a URL. */
  url?: string | Computed<string>;
  /** path parameter, not part of the API's own resource representation */
  botSlug: string | Computed<string>;
}

export interface BotAttrs {
  /** Can set the creator field with an internal user ID. */
  creator: string;
  /** An image binary data. Only needed when type is uploading a file. */
  file: string;
  /** Optional Image Custom ID. Up to 1024 chars. Can include any number of subpaths, and utf8 characters. Cannot start nor end with a / (forward slash). Cannot be a UUID. */
  id: string;
  /** User modifiable key-value store. Can use used for keeping references to another system of record for managing images. */
  metadata: unknown;
  /** Indicates whether the image requires a signature token for the access. */
  requireSignedUrls: boolean;
  result: Bot_Result;
  success: boolean;
  /** A URL to fetch an image from origin. Only needed when type is uploading from a URL. */
  url: string;
  /** path parameter, not part of the API's own resource representation */
  botSlug: string;
}

export const Bot: ResourceBinding<BotConfig, BotAttrs> = {
  wireType: "cloudflare_bot",
  fields: {
    creator: "creator",
    file: "file",
    id: "id",
    metadata: "metadata",
    requireSignedUrls: "require_signed_urls",
    url: "url",
    botSlug: "bot_slug",
  },
};

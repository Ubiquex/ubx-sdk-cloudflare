// Code generated by `ubx sdk gen` from cloudflare_histogram. DO NOT EDIT.
import type { Computed, FieldMap, DataSourceBinding } from "@ubx/sdk";

export interface Histogram_Result_Histogram0 {
  bandwidthDownload: string[] | Computed<string[]>;
  bandwidthUpload: string[] | Computed<string[]>;
  bucketMin: string[] | Computed<string[]>;
}

export interface Histogram_Result_Meta_ConfidenceInfo_Annotations {
  dataSource?: string | Computed<string>;
  description?: string | Computed<string>;
  endDate?: string | Computed<string>;
  eventType?: string | Computed<string>;
  isInstantaneous?: boolean | Computed<boolean>;
  linkedUrl?: string | Computed<string>;
  startDate?: string | Computed<string>;
  tags?: string[] | Computed<string[]>;
}

export interface Histogram_Result_Meta_ConfidenceInfo {
  annotations: Histogram_Result_Meta_ConfidenceInfo_Annotations[] | Computed<Histogram_Result_Meta_ConfidenceInfo_Annotations[]>;
  /** Provides an indication of how much confidence Cloudflare has in the data. */
  level: number | Computed<number>;
}

export interface Histogram_Result_Meta_DateRange {
  endTime?: string | Computed<string>;
  startTime?: string | Computed<string>;
}

export interface Histogram_Result_Meta_Units {
  name?: string | Computed<string>;
  value?: string | Computed<string>;
}

export interface Histogram_Result_Meta {
  /** The width for every bucket in the histogram. */
  bucketSize: number | Computed<number>;
  confidenceInfo: Histogram_Result_Meta_ConfidenceInfo | Computed<Histogram_Result_Meta_ConfidenceInfo>;
  dateRange: Histogram_Result_Meta_DateRange[] | Computed<Histogram_Result_Meta_DateRange[]>;
  /** Timestamp of the last dataset update. */
  lastUpdated: string | Computed<string>;
  /** Normalization method applied to the results. Refer to [Normalization methods](https://developers.cloudflare.com/radar/concepts/normalization/). */
  normalization: string | Computed<string>;
  totalTests: number[] | Computed<number[]>;
  /** Measurement units for the results. */
  units: Histogram_Result_Meta_Units[] | Computed<Histogram_Result_Meta_Units[]>;
}

export interface Histogram_Result {
  histogram0: Histogram_Result_Histogram0 | Computed<Histogram_Result_Histogram0>;
  /** Metadata for the results. */
  meta: Histogram_Result_Meta | Computed<Histogram_Result_Meta>;
}

export interface HistogramConfig {
  /** Filters results by Autonomous System. Specify one or more Autonomous System Numbers (ASNs) as a comma-separated list. Prefix with `-` to exclude ASNs from results. For example, `-174, 3356` excludes results from AS174, but includes results from AS3356. */
  asn?: string[] | Computed<string[]>;
  /** Specifies the width for every bucket in the histogram. */
  bucketSize?: number | Computed<number>;
  /** Filters results by continent. Specify a comma-separated list of alpha-2 codes. Prefix with `-` to exclude continents from results. For example, `-EU,NA` excludes results from EU, but includes results from NA. */
  continent?: string[] | Computed<string[]>;
  /** End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected. */
  dateEnd?: string[] | Computed<string[]>;
  /** Format in which results will be returned. */
  format?: string | Computed<string>;
  /** Filters results by location. Specify a comma-separated list of alpha-2 codes. Prefix with `-` to exclude locations from results. For example, `-US,PT` excludes results from the US, but includes results from PT. */
  location?: string[] | Computed<string[]>;
  /** Metrics to be returned. */
  metricGroup?: string | Computed<string>;
  /** Array of names used to label the series in the response. */
  name?: string[] | Computed<string[]>;
}

export interface HistogramAttrs {
  /** Filters results by Autonomous System. Specify one or more Autonomous System Numbers (ASNs) as a comma-separated list. Prefix with `-` to exclude ASNs from results. For example, `-174, 3356` excludes results from AS174, but includes results from AS3356. */
  asn: string[];
  /** Specifies the width for every bucket in the histogram. */
  bucketSize: number;
  /** Filters results by continent. Specify a comma-separated list of alpha-2 codes. Prefix with `-` to exclude continents from results. For example, `-EU,NA` excludes results from EU, but includes results from NA. */
  continent: string[];
  /** End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected. */
  dateEnd: string[];
  /** Format in which results will be returned. */
  format: string;
  /** Filters results by location. Specify a comma-separated list of alpha-2 codes. Prefix with `-` to exclude locations from results. For example, `-US,PT` excludes results from the US, but includes results from PT. */
  location: string[];
  /** Metrics to be returned. */
  metricGroup: string;
  /** Array of names used to label the series in the response. */
  name: string[];
  result: Histogram_Result;
  success: boolean;
}

export const Histogram: DataSourceBinding<HistogramConfig, HistogramAttrs> = {
  wireType: "cloudflare_histogram",
  fields: {
    asn: "asn",
    bucketSize: "bucket_size",
    continent: "continent",
    dateEnd: "date_end",
    format: "format",
    location: "location",
    metricGroup: "metric_group",
    name: "name",
  },
};

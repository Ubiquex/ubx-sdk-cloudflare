// Code generated by `ubx sdk gen` from cloudflare_edn. DO NOT EDIT.
import type { Computed, FieldMap, DataSourceBinding } from "@ubx/sdk";

export interface Edn_Result_Meta_ConfidenceInfo_Annotations {
  dataSource?: string | Computed<string>;
  description?: string | Computed<string>;
  endDate?: string | Computed<string>;
  eventType?: string | Computed<string>;
  isInstantaneous?: boolean | Computed<boolean>;
  linkedUrl?: string | Computed<string>;
  startDate?: string | Computed<string>;
  tags?: string[] | Computed<string[]>;
}

export interface Edn_Result_Meta_ConfidenceInfo {
  annotations: Edn_Result_Meta_ConfidenceInfo_Annotations[] | Computed<Edn_Result_Meta_ConfidenceInfo_Annotations[]>;
  /** Provides an indication of how much confidence Cloudflare has in the data. */
  level: number | Computed<number>;
}

export interface Edn_Result_Meta_DateRange {
  endTime?: string | Computed<string>;
  startTime?: string | Computed<string>;
}

export interface Edn_Result_Meta_Units {
  name?: string | Computed<string>;
  value?: string | Computed<string>;
}

export interface Edn_Result_Meta {
  confidenceInfo: Edn_Result_Meta_ConfidenceInfo | Computed<Edn_Result_Meta_ConfidenceInfo>;
  dateRange: Edn_Result_Meta_DateRange[] | Computed<Edn_Result_Meta_DateRange[]>;
  /** Timestamp of the last dataset update. */
  lastUpdated: string | Computed<string>;
  /** Normalization method applied to the results. Refer to [Normalization methods](https://developers.cloudflare.com/radar/concepts/normalization/). */
  normalization: string | Computed<string>;
  /** Measurement units for the results. */
  units: Edn_Result_Meta_Units[] | Computed<Edn_Result_Meta_Units[]>;
}

export interface Edn_Result_Summary0 {
  /** A numeric string. */
  notSupported: string | Computed<string>;
  /** A numeric string. */
  supported: string | Computed<string>;
}

export interface Edn_Result {
  /** Metadata for the results. */
  meta: Edn_Result_Meta | Computed<Edn_Result_Meta>;
  summary0: Edn_Result_Summary0 | Computed<Edn_Result_Summary0>;
}

export interface EdnConfig {
  /** Filters results by continent. Specify a comma-separated list of alpha-2 codes. Prefix with `-` to exclude continents from results. For example, `-EU,NA` excludes results from EU, but includes results from NA. */
  continent?: string[] | Computed<string[]>;
  /** End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected. */
  dateEnd?: string[] | Computed<string[]>;
  /** Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters). */
  dateRange?: string[] | Computed<string[]>;
  /** Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected. */
  dateStart?: string[] | Computed<string[]>;
  /** Format in which results will be returned. */
  format?: string | Computed<string>;
  /** Filters results by location. Specify a comma-separated list of alpha-2 codes. Prefix with `-` to exclude locations from results. For example, `-US,PT` excludes results from the US, but includes results from PT. */
  location?: string[] | Computed<string[]>;
  /** Array of names used to label the series in the response. */
  name?: string[] | Computed<string[]>;
  /** Filters results by DNS transport protocol. */
  protocol?: string[] | Computed<string[]>;
  /** Filters results by DNS query type. */
  queryType?: string[] | Computed<string[]>;
  /** Filters results by DNS response code. */
  responseCode?: string[] | Computed<string[]>;
}

export interface EdnAttrs {
  /** Filters results by continent. Specify a comma-separated list of alpha-2 codes. Prefix with `-` to exclude continents from results. For example, `-EU,NA` excludes results from EU, but includes results from NA. */
  continent: string[];
  /** End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected. */
  dateEnd: string[];
  /** Filters results by relative date range ending at the current time, with each value producing a separate series. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`). Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `7dcontrol` covers days -14 to -7, `10dcontrol` covers days -24 to -14). For example, pass `7d` and `7dcontrol` to compare this week with the previous week. All series must resolve to the same duration as the main series; relative ranges (including `control`) satisfy this automatically. Use this parameter or set specific start and end dates (`dateStart` and `dateEnd` parameters). */
  dateRange: string[];
  /** Start of the date range. Alternative to `dateRange`; provide together with `dateEnd`. When requesting comparison series, every series must resolve to the same duration as the main series. Each `dateStart`/`dateEnd` is floored to the nearest 15 minutes before evaluation, so windows whose durations match only before alignment may be rejected. */
  dateStart: string[];
  /** Format in which results will be returned. */
  format: string;
  /** Filters results by location. Specify a comma-separated list of alpha-2 codes. Prefix with `-` to exclude locations from results. For example, `-US,PT` excludes results from the US, but includes results from PT. */
  location: string[];
  /** Array of names used to label the series in the response. */
  name: string[];
  /** Filters results by DNS transport protocol. */
  protocol: string[];
  /** Filters results by DNS query type. */
  queryType: string[];
  /** Filters results by DNS response code. */
  responseCode: string[];
  result: Edn_Result;
  success: boolean;
}

export const Edn: DataSourceBinding<EdnConfig, EdnAttrs> = {
  wireType: "cloudflare_edn",
  fields: {
    continent: "continent",
    dateEnd: "date_end",
    dateRange: "date_range",
    dateStart: "date_start",
    format: "format",
    location: "location",
    name: "name",
    protocol: "protocol",
    queryType: "query_type",
    responseCode: "response_code",
  },
};

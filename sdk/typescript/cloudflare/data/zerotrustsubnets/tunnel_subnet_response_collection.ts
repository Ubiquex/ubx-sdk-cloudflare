// Code generated by `ubx sdk gen` from cloudflare_tunnel_subnet_response_collection. DO NOT EDIT.
import type { Computed, FieldMap, DataSourceBinding } from "@ubx/sdk";

export interface TunnelSubnetResponseCollection_Network {
}

export interface TunnelSubnetResponseCollection_Result_Capacity {
  total?: number | Computed<number>;
  used?: number | Computed<number>;
}

export interface TunnelSubnetResponseCollection_Result {
  capacity?: TunnelSubnetResponseCollection_Result_Capacity | Computed<TunnelSubnetResponseCollection_Result_Capacity>;
  comment?: string | Computed<string>;
  createdAt?: string | Computed<string>;
  deletedAt?: string | Computed<string>;
  id?: string | Computed<string>;
  isDefaultNetwork?: boolean | Computed<boolean>;
  name?: string | Computed<string>;
  network?: string | Computed<string>;
  subnetType?: string | Computed<string>;
}

const TunnelSubnetResponseCollection_NetworkFields: FieldMap = {
};

export interface TunnelSubnetResponseCollectionConfig {
  /** IP address family, either `v4` (IPv4) or `v6` (IPv6) */
  addressFamily?: string | Computed<string>;
  /** If set, only list subnets with the given comment. */
  comment?: string | Computed<string>;
  /** If provided, include only resources that were created (and not deleted) before this time. URL encoded. */
  existedAt?: string | Computed<string>;
  /** If `true`, only include default subnets. If `false`, exclude default subnets subnets. If not set, all subnets will be included. */
  isDefaultNetwork?: boolean | Computed<boolean>;
  /** If `true`, only include deleted subnets. If `false`, exclude deleted subnets. If not set, all subnets will be included. */
  isDeleted?: boolean | Computed<boolean>;
  /** If set, only list subnets with the given name */
  name?: string | Computed<string>;
  /** If set, only list the subnet whose network exactly matches the given CIDR. */
  network?: TunnelSubnetResponseCollection_Network | Computed<TunnelSubnetResponseCollection_Network>;
  /** Page number of paginated results. */
  page?: number | Computed<number>;
  /** Number of results to display. */
  perPage?: number | Computed<number>;
  /** Sort order of the results. `asc` means oldest to newest, `desc` means newest to oldest. If not set, they will not be in any particular order. */
  sortOrder?: string | Computed<string>;
  /** If set, the types of subnets to include, separated by comma. */
  subnetTypes?: string | Computed<string>;
}

export interface TunnelSubnetResponseCollectionAttrs {
  /** Cloudflare account ID */
  accountId: string;
  /** IP address family, either `v4` (IPv4) or `v6` (IPv6) */
  addressFamily: string;
  /** If set, only list subnets with the given comment. */
  comment: string;
  /** If provided, include only resources that were created (and not deleted) before this time. URL encoded. */
  existedAt: string;
  /** If `true`, only include default subnets. If `false`, exclude default subnets subnets. If not set, all subnets will be included. */
  isDefaultNetwork: boolean;
  /** If `true`, only include deleted subnets. If `false`, exclude deleted subnets. If not set, all subnets will be included. */
  isDeleted: boolean;
  /** If set, only list subnets with the given name */
  name: string;
  /** If set, only list the subnet whose network exactly matches the given CIDR. */
  network: TunnelSubnetResponseCollection_Network;
  /** Page number of paginated results. */
  page: number;
  /** Number of results to display. */
  perPage: number;
  result: TunnelSubnetResponseCollection_Result[];
  /** Sort order of the results. `asc` means oldest to newest, `desc` means newest to oldest. If not set, they will not be in any particular order. */
  sortOrder: string;
  /** If set, the types of subnets to include, separated by comma. */
  subnetTypes: string;
}

export const TunnelSubnetResponseCollection: DataSourceBinding<TunnelSubnetResponseCollectionConfig, TunnelSubnetResponseCollectionAttrs> = {
  wireType: "cloudflare_tunnel_subnet_response_collection",
  fields: {
    addressFamily: "address_family",
    comment: "comment",
    existedAt: "existed_at",
    isDefaultNetwork: "is_default_network",
    isDeleted: "is_deleted",
    name: "name",
    network: {
      wireName: "network",
      kind: "object",
      fields: TunnelSubnetResponseCollection_NetworkFields,
    },
    page: "page",
    perPage: "per_page",
    sortOrder: "sort_order",
    subnetTypes: "subnet_types",
  },
};

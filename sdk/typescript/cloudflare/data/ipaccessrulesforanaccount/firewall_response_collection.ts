// Code generated by `ubx sdk gen` from cloudflare_firewall_response_collection. DO NOT EDIT.
import type { Computed, FieldMap, DataSourceBinding } from "@ubx/sdk";

export interface FirewallResponseCollectionConfig {
  /** Defines an account identifier. */
  accountId: string | Computed<string>;
  /** Defines the target to search in existing rules. */
  configurationTarget?: string | Computed<string>;
  /** Defines the target value to search for in existing rules: an IP address, an IP address range, or a country code, depending on the provided `configuration.target`. Notes: You can search for a single IPv4 address, an IP address range with a subnet of '/16' or '/24', or a two-letter ISO-3166-1 alpha-2 country code. */
  configurationValue?: string | Computed<string>;
  /** Defines the direction used to sort returned rules. */
  direction?: string | Computed<string>;
  /** Defines the search requirements. When set to `all`, all the search requirements must match. When set to `any`, only one of the search requirements has to match. */
  match?: string | Computed<string>;
  /** The action to apply to a matched request. */
  mode?: string | Computed<string>;
  /** Defines the string to search for in the notes of existing IP Access rules. Notes: For example, the string 'attack' would match IP Access rules with notes 'Attack 26/02' and 'Attack 27/02'. The search is case insensitive. */
  notes?: string | Computed<string>;
  /** Defines the field used to sort returned rules. */
  order?: string | Computed<string>;
  /** Defines the requested page within paginated list of results. */
  page?: number | Computed<number>;
  /** Defines the maximum number of results requested. */
  perPage?: number | Computed<number>;
}

export interface FirewallResponseCollectionAttrs {
  /** Defines an account identifier. */
  accountId: string;
  /** Defines the target to search in existing rules. */
  configurationTarget: string;
  /** Defines the target value to search for in existing rules: an IP address, an IP address range, or a country code, depending on the provided `configuration.target`. Notes: You can search for a single IPv4 address, an IP address range with a subnet of '/16' or '/24', or a two-letter ISO-3166-1 alpha-2 country code. */
  configurationValue: string;
  /** Defines the direction used to sort returned rules. */
  direction: string;
  /** Defines the search requirements. When set to `all`, all the search requirements must match. When set to `any`, only one of the search requirements has to match. */
  match: string;
  /** The action to apply to a matched request. */
  mode: string;
  /** Defines the string to search for in the notes of existing IP Access rules. Notes: For example, the string 'attack' would match IP Access rules with notes 'Attack 26/02' and 'Attack 27/02'. The search is case insensitive. */
  notes: string;
  /** Defines the field used to sort returned rules. */
  order: string;
  /** Defines the requested page within paginated list of results. */
  page: number;
  /** Defines the maximum number of results requested. */
  perPage: number;
}

export const FirewallResponseCollection: DataSourceBinding<FirewallResponseCollectionConfig, FirewallResponseCollectionAttrs> = {
  wireType: "cloudflare_firewall_response_collection",
  fields: {
    accountId: "account_id",
    configurationTarget: "configuration_target",
    configurationValue: "configuration_value",
    direction: "direction",
    match: "match",
    mode: "mode",
    notes: "notes",
    order: "order",
    page: "page",
    perPage: "per_page",
  },
};

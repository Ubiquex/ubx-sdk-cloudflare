// Code generated by `ubx sdk gen` from cloudflare_tunnel_warp_connector_response_collection. DO NOT EDIT.
import type { Computed, FieldMap, DataSourceBinding } from "@ubx/sdk";

export interface TunnelWarpConnectorResponseCollection_Result_Connections_OriginIp {
}

export interface TunnelWarpConnectorResponseCollection_Result_Connections {
  clientId?: string | Computed<string>;
  clientVersion?: string | Computed<string>;
  coloName?: string | Computed<string>;
  id?: string | Computed<string>;
  isPendingReconnect?: boolean | Computed<boolean>;
  openedAt?: string | Computed<string>;
  originIp?: TunnelWarpConnectorResponseCollection_Result_Connections_OriginIp | Computed<TunnelWarpConnectorResponseCollection_Result_Connections_OriginIp>;
  uuid?: string | Computed<string>;
}

export interface TunnelWarpConnectorResponseCollection_Result {
  accountTag?: string | Computed<string>;
  connections?: TunnelWarpConnectorResponseCollection_Result_Connections[] | Computed<TunnelWarpConnectorResponseCollection_Result_Connections[]>;
  connsActiveAt?: string | Computed<string>;
  connsInactiveAt?: string | Computed<string>;
  createdAt?: string | Computed<string>;
  deletedAt?: string | Computed<string>;
  id?: string | Computed<string>;
  metadata?: unknown | Computed<unknown>;
  name?: string | Computed<string>;
  status?: string | Computed<string>;
  tunType?: string | Computed<string>;
}

export interface TunnelWarpConnectorResponseCollectionConfig {
  excludePrefix?: string | Computed<string>;
  /** If provided, include only resources that were created (and not deleted) before this time. URL encoded. */
  existedAt?: string | Computed<string>;
  includePrefix?: string | Computed<string>;
  /** If `true`, only include deleted tunnels. If `false`, exclude deleted tunnels. If empty, all tunnels will be included. */
  isDeleted?: boolean | Computed<boolean>;
  /** A user-friendly name for the tunnel. */
  name?: string | Computed<string>;
  /** Page number of paginated results. */
  page?: number | Computed<number>;
  /** Number of results to display. */
  perPage?: number | Computed<number>;
  /** The status of the tunnel. Valid values are `inactive` (tunnel has never been run), `degraded` (tunnel is active and able to serve traffic but in an unhealthy state), `healthy` (tunnel is active and able to serve traffic), or `down` (tunnel can not serve traffic as it has no connections to the Cloudflare Edge). */
  status?: string | Computed<string>;
  wasActiveAt?: string | Computed<string>;
  wasInactiveAt?: string | Computed<string>;
}

export interface TunnelWarpConnectorResponseCollectionAttrs {
  /** Cloudflare account ID */
  accountId: string;
  excludePrefix: string;
  /** If provided, include only resources that were created (and not deleted) before this time. URL encoded. */
  existedAt: string;
  includePrefix: string;
  /** If `true`, only include deleted tunnels. If `false`, exclude deleted tunnels. If empty, all tunnels will be included. */
  isDeleted: boolean;
  /** A user-friendly name for the tunnel. */
  name: string;
  /** Page number of paginated results. */
  page: number;
  /** Number of results to display. */
  perPage: number;
  result: TunnelWarpConnectorResponseCollection_Result[];
  /** The status of the tunnel. Valid values are `inactive` (tunnel has never been run), `degraded` (tunnel is active and able to serve traffic but in an unhealthy state), `healthy` (tunnel is active and able to serve traffic), or `down` (tunnel can not serve traffic as it has no connections to the Cloudflare Edge). */
  status: string;
  /** UUID of the tunnel. */
  uuid: string;
  wasActiveAt: string;
  wasInactiveAt: string;
}

export const TunnelWarpConnectorResponseCollection: DataSourceBinding<TunnelWarpConnectorResponseCollectionConfig, TunnelWarpConnectorResponseCollectionAttrs> = {
  wireType: "cloudflare_tunnel_warp_connector_response_collection",
  fields: {
    excludePrefix: "exclude_prefix",
    existedAt: "existed_at",
    includePrefix: "include_prefix",
    isDeleted: "is_deleted",
    name: "name",
    page: "page",
    perPage: "per_page",
    status: "status",
    wasActiveAt: "was_active_at",
    wasInactiveAt: "was_inactive_at",
  },
};

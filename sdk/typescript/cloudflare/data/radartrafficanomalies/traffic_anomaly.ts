// Code generated by `ubx sdk gen` from cloudflare_traffic_anomaly. DO NOT EDIT.
import type { Computed, FieldMap, DataSourceBinding } from "@ubx/sdk";

export interface TrafficAnomaly_Result_TrafficAnomalies_AsnDetails_Locations {
  code?: string | Computed<string>;
  name?: string | Computed<string>;
}

export interface TrafficAnomaly_Result_TrafficAnomalies_AsnDetails {
  asn?: string | Computed<string>;
  locations?: TrafficAnomaly_Result_TrafficAnomalies_AsnDetails_Locations | Computed<TrafficAnomaly_Result_TrafficAnomalies_AsnDetails_Locations>;
  name?: string | Computed<string>;
}

export interface TrafficAnomaly_Result_TrafficAnomalies_OriginDetails {
  name?: string | Computed<string>;
  origin?: string | Computed<string>;
}

export interface TrafficAnomaly_Result_TrafficAnomalies {
  asnDetails?: TrafficAnomaly_Result_TrafficAnomalies_AsnDetails | Computed<TrafficAnomaly_Result_TrafficAnomalies_AsnDetails>;
  endDate?: string | Computed<string>;
  locationDetails?: TrafficAnomaly_Result_TrafficAnomalies_AsnDetails_Locations | Computed<TrafficAnomaly_Result_TrafficAnomalies_AsnDetails_Locations>;
  originDetails?: TrafficAnomaly_Result_TrafficAnomalies_OriginDetails | Computed<TrafficAnomaly_Result_TrafficAnomalies_OriginDetails>;
  startDate?: string | Computed<string>;
  status?: string | Computed<string>;
  type?: string | Computed<string>;
  uuid?: string | Computed<string>;
  visibleInDataSources?: string[] | Computed<string[]>;
}

export interface TrafficAnomaly_Result {
  trafficAnomalies: TrafficAnomaly_Result_TrafficAnomalies[] | Computed<TrafficAnomaly_Result_TrafficAnomalies[]>;
}

export interface TrafficAnomalyConfig {
  /** Filters results by Autonomous System. Specify a single Autonomous System Number (ASN) as integer. */
  asn?: number | Computed<number>;
  /** End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. */
  dateEnd?: string | Computed<string>;
  /** Filters results by a relative date range ending at the current time. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`), e.g. `7d`. Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `3dcontrol` covers days -10 to -7, `7dcontrol` covers days -14 to -7, `28dcontrol` covers days -56 to -28, and `10dcontrol` covers days -24 to -14). Mutually exclusive with `dateStart`/`dateEnd`. */
  dateRange?: string | Computed<string>;
  /** Start of the date range (inclusive). Alternative to `dateRange`; provide together with `dateEnd`. */
  dateStart?: string | Computed<string>;
  /** Format in which results will be returned. */
  format?: string | Computed<string>;
  /** Limits the number of objects returned in the response. */
  limit?: number | Computed<number>;
  /** Filters results by location. Specify an alpha-2 location code. */
  location?: string | Computed<string>;
  /** Skips the specified number of objects before fetching the results. */
  offset?: number | Computed<number>;
  /** Filters results by origin. */
  origin?: string | Computed<string>;
  status?: string | Computed<string>;
  /** Filters results by entity type (LOCATION, AS, or ORIGIN). */
  type?: string[] | Computed<string[]>;
}

export interface TrafficAnomalyAttrs {
  /** Filters results by Autonomous System. Specify a single Autonomous System Number (ASN) as integer. */
  asn: number;
  /** End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. */
  dateEnd: string;
  /** Filters results by a relative date range ending at the current time. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`), e.g. `7d`. Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `3dcontrol` covers days -10 to -7, `7dcontrol` covers days -14 to -7, `28dcontrol` covers days -56 to -28, and `10dcontrol` covers days -24 to -14). Mutually exclusive with `dateStart`/`dateEnd`. */
  dateRange: string;
  /** Start of the date range (inclusive). Alternative to `dateRange`; provide together with `dateEnd`. */
  dateStart: string;
  /** Format in which results will be returned. */
  format: string;
  /** Limits the number of objects returned in the response. */
  limit: number;
  /** Filters results by location. Specify an alpha-2 location code. */
  location: string;
  /** Skips the specified number of objects before fetching the results. */
  offset: number;
  /** Filters results by origin. */
  origin: string;
  result: TrafficAnomaly_Result;
  status: string;
  success: boolean;
  /** Filters results by entity type (LOCATION, AS, or ORIGIN). */
  type: string[];
}

export const TrafficAnomaly: DataSourceBinding<TrafficAnomalyConfig, TrafficAnomalyAttrs> = {
  wireType: "cloudflare_traffic_anomaly",
  fields: {
    asn: "asn",
    dateEnd: "date_end",
    dateRange: "date_range",
    dateStart: "date_start",
    format: "format",
    limit: "limit",
    location: "location",
    offset: "offset",
    origin: "origin",
    status: "status",
    type: "type",
  },
};

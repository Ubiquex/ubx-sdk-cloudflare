// Code generated by `ubx sdk gen` from cloudflare_outage. DO NOT EDIT.
import type { Computed, FieldMap, DataSourceBinding } from "@ubx/sdk";

export interface Outage_Result_Annotations_AsnsDetails_Locations {
  code?: string | Computed<string>;
  name?: string | Computed<string>;
}

export interface Outage_Result_Annotations_AsnsDetails {
  asn?: string | Computed<string>;
  locations?: Outage_Result_Annotations_AsnsDetails_Locations | Computed<Outage_Result_Annotations_AsnsDetails_Locations>;
  name?: string | Computed<string>;
}

export interface Outage_Result_Annotations_OriginsDetails {
  name?: string | Computed<string>;
  origin?: string | Computed<string>;
}

export interface Outage_Result_Annotations_Outage {
  outageCause?: string | Computed<string>;
  outageType?: string | Computed<string>;
}

export interface Outage_Result_Annotations {
  asns?: number[] | Computed<number[]>;
  asnsDetails?: Outage_Result_Annotations_AsnsDetails[] | Computed<Outage_Result_Annotations_AsnsDetails[]>;
  dataSource?: string | Computed<string>;
  description?: string | Computed<string>;
  endDate?: string | Computed<string>;
  eventType?: string | Computed<string>;
  id?: string | Computed<string>;
  linkedUrl?: string | Computed<string>;
  locations?: string[] | Computed<string[]>;
  locationsDetails?: Outage_Result_Annotations_AsnsDetails_Locations[] | Computed<Outage_Result_Annotations_AsnsDetails_Locations[]>;
  origins?: string[] | Computed<string[]>;
  originsDetails?: Outage_Result_Annotations_OriginsDetails[] | Computed<Outage_Result_Annotations_OriginsDetails[]>;
  outage?: Outage_Result_Annotations_Outage | Computed<Outage_Result_Annotations_Outage>;
  scope?: string | Computed<string>;
  startDate?: string | Computed<string>;
}

export interface Outage_Result {
  annotations: Outage_Result_Annotations[] | Computed<Outage_Result_Annotations[]>;
}

export interface OutageConfig {
  /** Filters results by Autonomous System. Specify a single Autonomous System Number (ASN) as integer. */
  asn?: number | Computed<number>;
  /** End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. */
  dateEnd?: string | Computed<string>;
  /** Filters results by a relative date range ending at the current time. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`), e.g. `7d`. Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `3dcontrol` covers days -10 to -7, `7dcontrol` covers days -14 to -7, `28dcontrol` covers days -56 to -28, and `10dcontrol` covers days -24 to -14). Mutually exclusive with `dateStart`/`dateEnd`. */
  dateRange?: string | Computed<string>;
  /** Start of the date range (inclusive). Alternative to `dateRange`; provide together with `dateEnd`. */
  dateStart?: string | Computed<string>;
  /** Format in which results will be returned. */
  format?: string | Computed<string>;
  /** Limits the number of objects returned in the response. */
  limit?: number | Computed<number>;
  /** Filters results by location. Specify an alpha-2 location code. */
  location?: string | Computed<string>;
  /** Skips the specified number of objects before fetching the results. */
  offset?: number | Computed<number>;
  /** Filters results by origin. */
  origin?: string | Computed<string>;
}

export interface OutageAttrs {
  /** Filters results by Autonomous System. Specify a single Autonomous System Number (ASN) as integer. */
  asn: number;
  /** End of the date range (inclusive). Alternative to `dateRange`; provide together with `dateStart`. */
  dateEnd: string;
  /** Filters results by a relative date range ending at the current time. Use `<n>d` for days (up to `364d`) or `<n>w` for weeks (up to `52w`), e.g. `7d`. Append `control` to request the equivalent previous period for comparison: the comparison window is shifted back by the current window's length rounded up to a whole number of weeks, so it keeps the same weekday alignment and does not overlap the current window (e.g. `3dcontrol` covers days -10 to -7, `7dcontrol` covers days -14 to -7, `28dcontrol` covers days -56 to -28, and `10dcontrol` covers days -24 to -14). Mutually exclusive with `dateStart`/`dateEnd`. */
  dateRange: string;
  /** Start of the date range (inclusive). Alternative to `dateRange`; provide together with `dateEnd`. */
  dateStart: string;
  /** Format in which results will be returned. */
  format: string;
  /** Limits the number of objects returned in the response. */
  limit: number;
  /** Filters results by location. Specify an alpha-2 location code. */
  location: string;
  /** Skips the specified number of objects before fetching the results. */
  offset: number;
  /** Filters results by origin. */
  origin: string;
  result: Outage_Result;
  success: boolean;
}

export const Outage: DataSourceBinding<OutageConfig, OutageAttrs> = {
  wireType: "cloudflare_outage",
  fields: {
    asn: "asn",
    dateEnd: "date_end",
    dateRange: "date_range",
    dateStart: "date_start",
    format: "format",
    limit: "limit",
    location: "location",
    offset: "offset",
    origin: "origin",
  },
};

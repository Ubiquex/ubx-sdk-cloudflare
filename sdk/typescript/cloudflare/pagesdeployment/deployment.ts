// Code generated by `ubx sdk gen` from cloudflare_deployment. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface DeploymentConfig {
  /** Headers configuration file for the deployment. */
  headers?: string | Computed<string>;
  /** Redirects configuration file for the deployment. */
  redirects?: string | Computed<string>;
  /** Routes configuration file defining routing rules. */
  routesJson?: string | Computed<string>;
  /** Worker bundle file in multipart/form-data format. Mutually exclusive with `_worker.js`. Cannot specify both `_worker.js` and `_worker.bundle` in the same request. Maximum size: 25 MiB. */
  workerBundle?: string | Computed<string>;
  /** Worker JavaScript file. Mutually exclusive with `_worker.bundle`. Cannot specify both `_worker.js` and `_worker.bundle` in the same request. */
  workerJs?: string | Computed<string>;
  /** The branch to build the new deployment from. The `HEAD` of the branch will be used. If omitted, the production branch will be used by default. */
  branch?: string | Computed<string>;
  /** Boolean string indicating if the working directory has uncommitted changes. */
  commitDirty?: string | Computed<string>;
  /** Git commit SHA associated with this deployment. */
  commitHash?: string | Computed<string>;
  /** Git commit message associated with this deployment. */
  commitMessage?: string | Computed<string>;
  /** Functions routing configuration file. */
  functionsFilepathRoutingConfigJson?: string | Computed<string>;
  /** JSON string containing a manifest of files to deploy. Maps file paths to their content hashes. Required for direct upload deployments. Maximum 20,000 entries. */
  manifest?: string | Computed<string>;
  /** The build output directory path. */
  pagesBuildOutputDir?: string | Computed<string>;
  /** Hash of the Wrangler configuration file used for this deployment. */
  wranglerConfigHash?: string | Computed<string>;
  /** path parameter, not part of the API's own resource representation */
  accountId: string | Computed<string>;
  /** path parameter, not part of the API's own resource representation */
  projectName: string | Computed<string>;
  /** path parameter, not part of the API's own resource representation */
  deploymentId: string | Computed<string>;
}

export interface DeploymentAttrs {
  /** Headers configuration file for the deployment. */
  headers: string;
  /** Redirects configuration file for the deployment. */
  redirects: string;
  /** Routes configuration file defining routing rules. */
  routesJson: string;
  /** Worker bundle file in multipart/form-data format. Mutually exclusive with `_worker.js`. Cannot specify both `_worker.js` and `_worker.bundle` in the same request. Maximum size: 25 MiB. */
  workerBundle: string;
  /** Worker JavaScript file. Mutually exclusive with `_worker.bundle`. Cannot specify both `_worker.js` and `_worker.bundle` in the same request. */
  workerJs: string;
  /** The branch to build the new deployment from. The `HEAD` of the branch will be used. If omitted, the production branch will be used by default. */
  branch: string;
  /** Boolean string indicating if the working directory has uncommitted changes. */
  commitDirty: string;
  /** Git commit SHA associated with this deployment. */
  commitHash: string;
  /** Git commit message associated with this deployment. */
  commitMessage: string;
  /** Functions routing configuration file. */
  functionsFilepathRoutingConfigJson: string;
  /** JSON string containing a manifest of files to deploy. Maps file paths to their content hashes. Required for direct upload deployments. Maximum 20,000 entries. */
  manifest: string;
  /** The build output directory path. */
  pagesBuildOutputDir: string;
  /** Hash of the Wrangler configuration file used for this deployment. */
  wranglerConfigHash: string;
  /** path parameter, not part of the API's own resource representation */
  accountId: string;
  /** path parameter, not part of the API's own resource representation */
  projectName: string;
  /** path parameter, not part of the API's own resource representation */
  deploymentId: string;
}

export const Deployment: ResourceBinding<DeploymentConfig, DeploymentAttrs> = {
  wireType: "cloudflare_deployment",
  fields: {
    headers: "_headers",
    redirects: "_redirects",
    routesJson: "_routes_json",
    workerBundle: "_worker_bundle",
    workerJs: "_worker_js",
    branch: "branch",
    commitDirty: "commit_dirty",
    commitHash: "commit_hash",
    commitMessage: "commit_message",
    functionsFilepathRoutingConfigJson: "functions_filepath_routing_config_json",
    manifest: "manifest",
    pagesBuildOutputDir: "pages_build_output_dir",
    wranglerConfigHash: "wrangler_config_hash",
    accountId: "account_id",
    projectName: "project_name",
    deploymentId: "deployment_id",
  },
};

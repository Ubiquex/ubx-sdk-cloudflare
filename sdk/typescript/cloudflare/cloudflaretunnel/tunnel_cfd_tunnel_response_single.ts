// Code generated by `ubx sdk gen` from cloudflare_tunnel_cfd_tunnel_response_single. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface TunnelCfdTunnelResponseSingle_Errors {
  code?: number | Computed<number>;
  message?: string | Computed<string>;
}

export interface TunnelCfdTunnelResponseSingle_Result_Connections_OriginIp {
}

export interface TunnelCfdTunnelResponseSingle_Result_Connections {
  clientId?: string | Computed<string>;
  clientVersion?: string | Computed<string>;
  coloName?: string | Computed<string>;
  id?: string | Computed<string>;
  isPendingReconnect?: boolean | Computed<boolean>;
  openedAt?: string | Computed<string>;
  originIp?: TunnelCfdTunnelResponseSingle_Result_Connections_OriginIp | Computed<TunnelCfdTunnelResponseSingle_Result_Connections_OriginIp>;
  uuid?: string | Computed<string>;
}

export interface TunnelCfdTunnelResponseSingle_Result {
  /** Cloudflare account ID */
  accountTag?: string | Computed<string>;
  /** Indicates if this is a locally or remotely configured tunnel. If `local`, manage the tunnel using a YAML file on the origin machine. If `cloudflare`, manage the tunnel on the Zero Trust dashboard. */
  configSrc?: string | Computed<string>;
  /** The Cloudflare Tunnel connections between your origin and Cloudflare's edge. */
  connections?: TunnelCfdTunnelResponseSingle_Result_Connections[] | Computed<TunnelCfdTunnelResponseSingle_Result_Connections[]>;
  /** Timestamp of when the tunnel established at least one connection to Cloudflare's edge. If `null`, the tunnel is inactive. */
  connsActiveAt?: string | Computed<string>;
  /** Timestamp of when the tunnel became inactive (no connections to Cloudflare's edge). If `null`, the tunnel is active. */
  connsInactiveAt?: string | Computed<string>;
  /** Timestamp of when the resource was created. */
  createdAt?: string | Computed<string>;
  /** Timestamp of when the resource was deleted. If `null`, the resource has not been deleted. */
  deletedAt?: string | Computed<string>;
  /** UUID of the tunnel. */
  id?: string | Computed<string>;
  /** Metadata associated with the tunnel. */
  metadata?: unknown | Computed<unknown>;
  /** A user-friendly name for a tunnel. */
  name?: string | Computed<string>;
  /** If `true`, the tunnel can be configured remotely from the Zero Trust dashboard. If `false`, the tunnel must be configured locally on the origin machine. */
  remoteConfig?: boolean | Computed<boolean>;
  /** The status of the tunnel. Valid values are `inactive` (tunnel has never been run), `degraded` (tunnel is active and able to serve traffic but in an unhealthy state), `healthy` (tunnel is active and able to serve traffic), or `down` (tunnel can not serve traffic as it has no connections to the Cloudflare Edge). */
  status?: string | Computed<string>;
  /** The type of tunnel. */
  tunType?: string | Computed<string>;
}

export interface TunnelCfdTunnelResponseSingleConfig {
  /** Indicates if this is a locally or remotely configured tunnel. If `local`, manage the tunnel using a YAML file on the origin machine. If `cloudflare`, manage the tunnel on the Zero Trust dashboard. */
  configSrc?: string | Computed<string>;
  /** A user-friendly name for a tunnel. */
  name: string | Computed<string>;
  /** Sets the password required to run a locally-managed tunnel. Must be at least 32 bytes and encoded as a base64 string. */
  tunnelSecret?: string | Computed<string>;
  /** path parameter, not part of the API's own resource representation */
  accountId: string | Computed<string>;
  /** path parameter, not part of the API's own resource representation */
  tunnelId: string | Computed<string>;
}

export interface TunnelCfdTunnelResponseSingleAttrs {
  /** Indicates if this is a locally or remotely configured tunnel. If `local`, manage the tunnel using a YAML file on the origin machine. If `cloudflare`, manage the tunnel on the Zero Trust dashboard. */
  configSrc: string;
  errors: TunnelCfdTunnelResponseSingle_Errors[];
  messages: TunnelCfdTunnelResponseSingle_Errors[];
  /** A user-friendly name for a tunnel. */
  name: string;
  /** A Cloudflare Tunnel that connects your origin to Cloudflare's edge. */
  result: TunnelCfdTunnelResponseSingle_Result;
  /** Whether the API call was successful */
  success: boolean;
  /** Sets the password required to run a locally-managed tunnel. Must be at least 32 bytes and encoded as a base64 string. */
  tunnelSecret: string;
  /** path parameter, not part of the API's own resource representation */
  accountId: string;
  /** path parameter, not part of the API's own resource representation */
  tunnelId: string;
}

export const TunnelCfdTunnelResponseSingle: ResourceBinding<TunnelCfdTunnelResponseSingleConfig, TunnelCfdTunnelResponseSingleAttrs> = {
  wireType: "cloudflare_tunnel_cfd_tunnel_response_single",
  fields: {
    configSrc: "config_src",
    name: "name",
    tunnelSecret: "tunnel_secret",
    accountId: "account_id",
    tunnelId: "tunnel_id",
  },
};

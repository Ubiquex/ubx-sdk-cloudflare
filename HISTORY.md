# HISTORY.md -- narrative archive

> Consulted only when a session needs to know why a decision was made, not on
> every open. For what's current, read `STATE.md` instead.

This repo was just scaffolded -- no real history yet. Real history for
Cloudflare's own onboarding into `ubx` lives in `ubiquex`'s own
`STATE.md`/`HISTORY.md` (search for Cloudflare) until this repo
accumulates its own.

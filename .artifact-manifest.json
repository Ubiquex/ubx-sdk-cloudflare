{
  "runbook": "write-artifacts",
  "target": "cloudflare",
  "started": "2026-09-03",
  "updated": "2026-09-03",
  "hops": [
    {
      "id": "categories",
      "status": "done",
      "commit": "https://github.com/Ubiquex/ubx-sdk-cloudflare/pull/1",
      "note": "UBI-245 token method applied against a genuinely empty categories.json (real classification: 0 clean, 0 ambiguous, 444 no-signal, reported before authoring anything). Real signal used: Cloudflare's own 551 human-authored OpenAPI tags, the same signal namespace_from_tags already uses for wire grouping. 426 of 444 tokens normalized directly from real tag text; 18 carried an internal non-human tag and were individually traced to real wire types/paths, collapsing onto 9 real Cloudflare products. Real result: 433 distinct category labels for 1088 wire types, 100% coverage verified both by direct computation and a real ubx-docs-providers sidebar build against a local mirror. Not forced coarser than the real product taxonomy warrants. One key (cloudflare_location) later removed once the wire-type set itself changed -- see write-intros hop's own notes."
    },
    {
      "id": "write-intros",
      "status": "in_progress",
      "note": "Real wire-type set is now 233 resources + 823 data sources (1056 total) -- two ubx-provider-dynamic create-binding fixes landed since categories was authored (see sdk/providers/.onboarding/cloudflare.json in ubiquex for the full account), dropping 268 -> 233 real resources. categories.json's own coverage re-verified against the new set: 966 of 967 keys still needed (cloudflare_location, which never had a real create OR real data source once both fixes landed, removed).\n\nBatch 1 (commit 70de50c, merged via PR #2): 50/1056 wires. cloudflare_commit is really an Artifacts repository; cloudflare_cost is really an AI Gateway custom-provider registration.\n\nBatch 2 (commit 0e1fbbb, merged via PR #3): 60/1056 wires (110 total). cloudflare_dataset/cloudflare_evaluation are the same AI Gateway entity; cloudflare_item is really an AI Search namespace; cloudflare_job is really the same AI Search instance as cloudflare_instance.\n\nBatch 3 (commit c49d2f5, merged via PR #4): 60/1056 wires (170 total). cloudflare_log/cloudflare_route are two more AI Gateway duplicates. cloudflare_memory/cloudflare_namespace confirmed identical. cloudflare_repo confirmed the same Artifacts entity as cloudflare_commit.\n\nBatch 4 (commit 95f1fbb, on branch artifacts/intros-batch-4): 63/1056 wires (233 total) -- ALL 233 REAL RESOURCES NOW HAVE AN INTRO, verified by direct set-difference against the authoritative dump-ir resource list (zero missing). Final AI Gateway duplicates found: cloudflare_url, cloudflare_version. cloudflare_tree confirmed the same Artifacts entity as cloudflare_commit/cloudflare_repo (three generated names, one real endpoint).\n\nReal, complete list of AI Gateway generated-name duplicates, all binding to POST /accounts/{account_id}/ai-gateway/gateways: cloudflare_gateway (correctly named), cloudflare_cost, cloudflare_custom_domain, cloudflare_custom_provider, cloudflare_dataset, cloudflare_evaluation, cloudflare_log, cloudflare_route, cloudflare_url, cloudflare_version -- 10 generated names for ONE real product. Real, complete list of Artifacts-repository duplicates: cloudflare_commit, cloudflare_repo, cloudflare_tree -- 3 generated names for ONE real endpoint. Both real, worth a dedicated noun-quality/collision-resolution pass in ubx-provider-dynamic at some point, out of this session's own scope (create-binding correctness and content authoring, not noun derivation).\n\nRemaining: 823/1056 wires still need an intro (all data sources -- every resource is done). 2178 depth-0 field descriptions also still missing -- not started yet, intros were the more expensive, non-batchable-for-free work per write-artifacts.md's own framing, prioritized first."
    }
  ]
}
